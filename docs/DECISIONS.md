# 결정 기록

기획서와 달라진 결정만 적습니다. 한 줄이면 충분합니다.
날짜 · 무엇이 바뀌었는지 · 왜.

기획서를 다시 손볼 때 이 파일을 보고 반영합니다.

---

- 2026-09-19 · 아키텍처를 CMP + Koin으로 확정, 출시는 안드로이드만. iOS는 TestFlight
  외부 테스트에 Beta App Review가 걸려 배포 경로가 막혀 있다. 공유 코드(data, domain)에서
  CMP 값어치를 가져가고 UI는 안드로이드부터 낸다.
- 2026-09-19 · 무기 스킨별 통계는 넣지 않는다. 경기 응답의 `damageItem`은 총기 종류
  ID라 스킨을 못 가리고, 로드아웃은 공식 API 밖이다. 설령 받아도 탄도가 같아 차이는
  표본 노이즈다.
- 2026-09-19 · 서체를 미정으로 되돌렸다. 목업의 IBM Plex Sans KR은 구글 폰트에서만
  불러올 수 있는 아트보드 제약 때문에 고른 것이지 제품 결정이 아니었다. Pretendard가
  유력. 구현 시점에 정하고 이 파일에 다시 적는다.
- 2026-09-19 · 커밋과 문서 규칙을 `docs/CONVENTIONS.md`로 분리했다. 커밋은 Conventional
  Commits에 `ui` 타입과 `Decision:` 트레일러를 더한 형태. `.githooks/pre-commit`이
  경고로만 띄우고 넘어가던 문서 동기화 여부를 커밋 이력에도 남기려고.
- 2026-09-19 · 말투를 갈랐다. `CLAUDE.md`는 단정형, `docs/` 아래 산문은 존댓말.
  규칙집은 짧게 끊어야 규칙으로 읽히고, 사람이 읽는 문서는 존댓말이 맞다.
- 2026-09-19 · 서체를 Pretendard Variable 한 벌로 확정(v1.3.9, SIL OFL). 숫자는 Archivo로
  나누지 않고 Pretendard의 tabular figures로 묶는다. 위계를 이미 밝기 단계와 굵기로 잡고
  있어 라틴 서체를 하나 더 올리면 오히려 탁해진다. 가변 폰트라 파일 하나로 9굵기를 쓴다.
  `docs/FIRST-TASK.md`의 "Archivo + IBM Plex Sans KR"은 이 결정으로 대체됐다.
- 2026-09-19 · 로컬 DB 선택을 미룬다. `CLAUDE.md`에 아직 못 확인한 응답 형태가 셋 남아
  있고 프로덕션 키도 안 나왔다. 스키마를 모르는 채 고르면 두 번 일한다. `core/data`에는
  Repository 인터페이스와 가짜 구현만 두고, 키가 나온 뒤 Room KMP와 SQLDelight를 놓고
  정한다. `CLAUDE.md`의 "버릴 것: Room + FTS"는 nowinandroid의 FTS 전문 검색을 안 쓴다는
  뜻으로 좁혔다. Room 자체를 막는 규칙이 아니다.
- 2026-09-19 · 버전 조합을 Kotlin 2.4.20 · CMP 1.12.0 · AGP 9.4.1 · Gradle 9.7.1로 잡았다.
  AGP 9부터 KMP 공유 모듈이 `com.android.library`와 호환되지 않아 전부
  `com.android.kotlin.multiplatform.library`로 간다. 이 플러그인은 빌드 변형이 없어
  공유 모듈에서 `debugImplementation`을 못 쓴다. 도구 의존성은 `androidMain`에 둔다.
  → 2026-09-20에 AGP를 9.3.0으로 내렸다. Android Studio 2026.1이 9.3까지만 받는다.
  9.3.0도 compileSdk 37까지 지원해서 잃는 건 없다.
- 2026-09-19 · 돈이 드는 선택지를 전부 뺀다. 백엔드는 무료 한도 안에서 돌고 카드 등록이
  필요 없는 곳만 쓴다. 라이선스도 상업 이용이 무료인 것만. 피할 수 없는 비용은 Google
  Play 개발자 등록 25달러 하나뿐이고 출시 직전에만 든다. `CLAUDE.md`에 비용 항목을 새로
  넣었다.
- 2026-09-20 · 서체를 Pretendard Variable v1.3.9로 실었다(6.4MB, SIL OFL). 정적 3종을
  받으면 7.7MB라 가변 쪽이 오히려 작다. 굵기마다 `wght` 축 값을 같이 넘겨야 진짜 굵기로
  그려진다. 안 넘기면 전부 기본 굵기가 나오고 시스템이 가짜 볼드를 만든다. 그전까지는
  시스템 기본 폰트라 안드로이드에서 Roboto가 나오고 있었고, 이건 "피하는 것" 목록 위반이었다.
- 2026-09-20 · 인트로 카피를 "오발있?"으로 연다. 로고가 초성 ㅇㅂㅇ라 처음 보는 사람은
  읽는 법을 모르는데, 바로 밑에서 소리 내어 읽어주는 역할을 겸한다. 다만 "오발있?"(같이
  하자는 말)과 전적 얘기는 화행이 달라서 그냥 붙여 두면 슬로건 두 개가 따로 논다.
  "그전에"로 이어 한 문장으로 만들었다.
- 2026-09-20 · 비공식 고지를 Riot의 "Legal Jibber Jabber" 지정 문구로 바꿨다. 그전에는
  League 계열 사이트들이 관행적으로 쓰는 "승인을 받지 않았으며..." 쪽을 옮겨 적었는데,
  Riot이 문서에 올려둔 문구와 다르다. 에셋을 쓰는 이상 지정 문구를 그대로 써야 한다.
  → 2026-09-25에 되돌렸다. 방향을 반대로 잡은 결정이었다. 아래 항목 참고.
- 2026-09-20 · 한국어 줄바꿈을 `LineBreak.Heading`으로 고정했다. 기본값은 한글을 글자
  단위로 잘라 "공식적으 / 로"처럼 어절이 쪼개진다. Heading은 어절 경계를 지키고(Phrase)
  마지막 한 단어만 다음 줄로 떨어지는 것도 막는다(Balanced). 조합을 직접 넘기는
  `LineBreak(...)` 생성자는 안드로이드 전용이라 commonMain에서 못 쓴다. Phrase는 글자의 언어가
  한국어일 때만 걸리므로 `localeList`도 ko-KR로 못박았다. 폰을 영어로 쓰는 한국 유저가
  흔한데 그때 기기 로케일을 따라가면 줄바꿈이 다시 깨진다.
- 2026-09-20 · 스플래시 애니메이션은 넣지 않는다. 두 가지를 만들어 보고 뺐다. 다시 시도할
  생각이면 아래를 먼저 읽기.

  ㅂ이 내려앉는 동작을 먼저 넣었는데 아예 안 보였다. 시스템이 앱 준비 시점에 스플래시를
  끊기 때문이다. 획을 순서대로 그리는 동작으로 바꾸고 `setKeepOnScreenCondition`으로
  860ms 붙잡으니 그제야 보였다. 즉 보이게 하려면 **앱 시작을 일부러 늦춰야 하고, 중간은
  없다.** 붙잡거나 애니메이션이 없는 것과 같거나다.

  0.9초를 브랜드에 쓰는 값어치가 없다고 봤다. 이 앱은 주 1회 여는 앱이라 첫인상보다
  들어가서 보는 숫자가 중요하다. 스플래시는 정적 마크로 둔다.
- 2026-09-20 · 스플래시 배경도 `--accent`로 바꿔 런처 아이콘과 맞췄다. 그 전에는 시스템
  테마를 따르게 해뒀는데, 그렇게 잡은 근거("스플래시는 앱 첫 화면으로 매끄럽게 이어져야
  한다")가 약했다. 참고 앱인 토스와 당근이 둘 다 브랜드 색 스플래시를 쓰고, Android
  스플래시는 사라질 때 페이드가 들어가서 색이 바뀌는 게 잘 안 느껴진다. 이제 테마를 따르는
  건 창 배경뿐이다. 색 이름도 `splash_*`/`launcher_*`에서 `brand_surface`/`brand_mark`로
  합쳤다.
- 2026-09-20 · 런처 아이콘 바탕을 `--bg`에서 `--accent`로 바꿨다. 처음에는 배경색을 그대로
  가져다 썼는데 런처에서 검은 덩어리로만 보였다. 아이콘은 면을 칠하는 자리라 "accent는 버튼
  면일 때만 금색 그대로 쓴다"는 규칙이 그대로 적용된다. `--on-accent`와의 대비는 색 토큰
  테스트가 이미 검증하고 있다. 화면 안의 로고는 글자에 가까우므로 `--t1`을 유지한다.
- 2026-09-20 · 적응형 아이콘 마크를 70dp에서 50dp로 줄였다. 108dp 캔버스 기준 65%였지만
  런처가 실제로 보여주는 건 지름 72dp 마스크뿐이라 그 안에서 92%를 차지했다. 잘리는
  한계(75dp)와 보기 좋은 크기는 다른 값이었다. 스플래시는 규격이 288dp로 달라 드로어블을
  따로 뒀다. 색은 아래 항목에서 금색으로 맞췄다.
- 2026-09-20 · 화면 문구를 Kotlin 상수에서 CMP 리소스로 옮겼다. 상수로 두면 로케일 분기가
  안 되고, 화면이 스무 개로 늘어난 뒤에 걷어내면 훨씬 비싸다. 모듈별
  `composeResources/values/strings.xml`을 쓰고 `values-en`은 필요할 때 옆에 둔다.
  번역은 아직 안 넣는다. 용어가 한글 전용이라 영어 대응어부터 정해야 한다.
- 2026-09-20 · UI 카피 말투를 해요체로 정했다. 참고하는 토스·당근이 해요체이고, 주간
  리포트가 말을 거는 형식이라 합니다체는 거리가 생긴다. 법적 고지만 합니다체.
- 2026-09-20 · CMP 1.12에서 `compose.runtime` 축약 접근자와
  `org.jetbrains.compose.ui.tooling.preview.Preview`가 deprecated됐다. 카탈로그에 좌표를
  직접 넣고 `@Preview`는 `androidx.compose.ui.tooling.preview` 쪽으로 옮겼다. 후자는 크기와
  fontScale 인자를 받아 작은 기기와 큰 글자 확인이 된다. material3만 버전이 1.9.0으로 갈라졌다.
- 2026-09-20 · 테스트를 `commonTest`에 둔다. 한 번 쓰면 안드로이드와 iOS 양쪽에서 돈다.
  처음엔 SDK 37이 없어 iOS 시뮬레이터에서만 돌렸다. AGP의 KMP 라이브러리 플러그인은 단위
  테스트가 opt-in이라 컨벤션 플러그인에 `withHostTestBuilder`를 켜뒀다. 이 한 줄이 없으면
  `androidHostTest` 소스셋 자체가 안 생긴다.
- 2026-09-23 · 지표 계산을 API 응답 모양이 아니라 도메인 모델 위에 먼저 짰다. 응답에는
  아직 확인 못 한 부분이 셋 남아 있고 프로덕션 키도 없다. 계산 규칙은 응답 모양과 무관하니
  지금 테스트로 굳히고, 키가 나오면 네트워크 계층이 응답을 도메인으로 옮겨 담게 한다.
- 2026-09-23 · 트레이드 창을 5초로 잡았다. 기획서에 값이 없고 공식 값도 없다. 동적 지표
  기준선(표준편차 1.5배)과 같은 취급이다. 시작 기준선이고 실데이터로 조정한다.
- 2026-09-23 · 코드 식별자는 영어, 화면 이름은 한글로 갈랐다. CLAUDE.md 용어 표는 화면과
  문서에 쓰는 말이다. 한글 식별자는 스택 트레이스와 도구에서 다루기 어렵다.
- 2026-09-23 · 퍼블 계열 세 지표를 정의했다. `CLAUDE.md`에는 이름만 있고 계산이 없었다.
  퍼블 승률은 내가 퍼블을 딴 라운드 중 이긴 비율로 잡았다. 우리 팀 퍼블 기준으로 세면 내가
  아무것도 안 해도 팀원이 잘하면 오른다. 개인 지표 앱에서 기본 3개에 들어갈 숫자는 아니다.
  퍼블 관여율은 (퍼블 + 퍼데) ÷ 라운드, 첫 교전 승률은 퍼블 ÷ (퍼블 + 퍼데). 퍼블 관여율에
  첫 킬 어시는 넣지 않았다. 첫 교전에 직접 들어갔는지를 보는 숫자라서.
- 2026-09-25 · 주를 달력 주로 잡았다. 기기 시간대 기준 월요일 0시에 바뀐다. 최근 7일처럼
  굴러가는 창은 새 경기 없이도 숫자가 매일 바뀐다. 끝난 주의 숫자는 그대로여야 믿는다.
  월요일 아침에는 이번 주가 비지만, 5경기 규칙이 지난주까지 넓혀서 자연스럽게 지난주
  리포트가 뜬다.
- 2026-09-25 · 집계 기간을 최대 4주까지 넓힌다. `CLAUDE.md`에는 `최근 2주`라는 예만 있었다.
  2주에서 끊으면 주에 두 판 하는 사람은 리포트를 영영 못 본다. 집계 4주와 비교 기준 4주를
  합치면 첫 수집 범위 8주와 맞는다. 4주로도 모자라면 리포트 대신 뛴 경기 수를 보여준다.
- 2026-09-25 · 비교 기준("지난 4주 평균")도 5경기 미만이면 비교하지 않는다. 한두 판짜리
  평균과 견주면 변화가 부풀려진다.
- 2026-09-25 · 리포트는 가장 최근 경기의 액트만 센다. 새 액트 초반에는 5경기 전까지
  리포트가 없다. 이전 액트 리포트를 대신 띄우는 방법도 있지만 "이번 주"라는 말과 맞지 않는다.
- 2026-09-25 · 기본 3개의 헤드샷을 관여율로 바꿨다. 헤드샷은 고정 4개에도 있어서 변화가 없는
  주에는 한 화면에 두 번 떴다. 관여율은 역할과 상관없이 읽혀서 빈칸 채우기에 맞다. 같은
  이유로 고정 4개는 동적 칸 후보에서도 뺐다.
- 2026-09-25 · 동적 지표 최소 표본을 라운드 40, 퍼블 10번, 첫 교전 15번으로 잡았다. 변동폭은
  표본을 넘긴 주로만 재고, 그런 주가 4주 미만이면 판단하지 않는다. 한두 판짜리 주가 섞이면
  변동폭이 부풀어 진짜 변화가 묻힌다. 트레이드 5초처럼 실데이터로 조정할 시작값이다.
- 2026-09-25 · 역할군을 동적 칸에 반영했다. 그 주의 역할은 라운드를 가장 많이 뛴 역할이다.
  경기 수로 세면 짧게 끝난 판이 같은 무게를 받는다. 크게 띄우지 않는 지표는 빈칸 채우기에서도
  빼서, 전략가와 감시자에게는 기본 3개에서도 퍼블 승률이 빠진다.
- 2026-09-25 · 기준을 넘긴 지표가 1~2개면 그걸 앞에 두고 남은 칸을 기본 지표로 채운다.
  `CLAUDE.md`는 "3개 미만이면 큰 변화 없음"이라 적혀 있어 움직인 지표를 버리는 걸로 읽혔다.
  "큰 변화 없음"은 하나도 안 움직였을 때만 띄우고, 기록이 모자라 판단을 못 한 경우와 구분한다.
- 2026-09-25 · 홈에서 `--pos`/`--neg`는 동적 칸에서 움직인 지표에만 칠한다. 고정 4칸은
  변동폭 판단을 거치지 않아서, 거기에 색을 칠하면 헤드샷 1%p 하락도 빨갛게 떠 경고처럼 읽혔다.
  퍼블 관여율은 역할마다 좋고 나쁨이 갈려서 색을 칠하지 않는다.
  → 같은 날 고정 칸 부분을 되돌렸다. 목업을 안 보고 정한 것이었고, 목업은 고정 칸 변화량도
  칠한다. 동적 칸에서 평소 범위 안의 변화를 무채색으로 두는 것과 퍼블 관여율 예외는 남겼다.
- 2026-09-25 · 동적 칸 근거에서 경기 수를 뺐다. 기획 예시는 `11경기, 판당 16킬`처럼 칸마다
  표본을 적는데, 헤드라인에 경기 수가 이미 있어서 세 칸에 같은 숫자가 되풀이됐다. 분모가 경기
  수와 다른 퍼블 승률과 첫 교전 승률만 표본을 적는다.
  → 같은 날 되돌렸다. 목업은 칸마다 표본(`라운드 496`)을 적는다. 목업을 안 보고 정한 것이었다.
- 2026-09-25 · Navigation 3를 1.1.7에서 1.2.0으로 올리고 `composeApp`에 붙였다. 1.2.0이 지금
  최신 안정판이다. 연동 동의와 로그인이 아직 없어서 인트로가 가짜 데이터로 홈을 바로 연다.
- 2026-09-25 · 비공식 고지 문구를 개발자 포털 General Policies의 Core Policies 영문 원문으로
  바꿨다. 09-20에 옮긴 "Legal Jibber Jabber" 문구는 팬 프로젝트용이고, Riot API를 쓰는 제품은
  Core Policies 문구가 의무다. 제출한 신청서에도 "standard not-endorsed disclaimer"를 단다고
  적었다. 번역하면 지정 문구를 바꿔 쓰는 셈이라 영문 그대로 둔다. OP.GG도 한국어 사이트에
  영문으로 둔다. 제품 이름 자리에는 신청서의 영문 이름 Ovalit을 넣었다.
- 2026-09-25 · 비공식 고지 위치를 인트로와 S4 설정 하단으로 정하고 홈에서 뺐다. 정책은
  "플레이어가 언제든 찾을 수 있는 자리"만 요구한다. 최초 로그인에 한 번 보여주는 것으로는 부족하고,
  모든 화면에 둘 필요도 없다. 심사에 낸 목업이 설정 하단에 뒀다. 설정 화면은 아직 없어서 지금은
  인트로에만 있다. 설정은 출시 전에 반드시 있어야 하는 화면이라 그때 같이 들어간다.
- 2026-09-25 · 홈을 목업 기준으로 다시 짰다. 처음에는 목업을 안 보고 `CLAUDE.md` 규칙만으로
  만들어서 구조가 달랐다. 지금 계산할 수 있는 부분(큐 칩, 기간 헤더, 고정 4칸 한 줄, 판당 K/D/A와
  4주 평균 줄, 달라진 점 가로 칸)은 목업대로 옮겼다. 하단 탭바, `›`, 개선 포인트, 라이벌, 친구
  비교, 티어는 갈 화면이나 데이터가 생기면 붙인다. 누를 곳이 없는 화살표와 탭은 미리 그리지 않는다.
  목업에서 `--t4`로 쓴 보조 글자는 대비가 모자라 `--t3`로 올렸다. 서체와 로고는 목업 이후의
  결정(Pretendard, ㅇㅂㅇ)을 따른다.
- 2026-09-25 · 이번 주에 뛴 경기가 없으면 지난주에서 끝나는 기간으로 리포트를 만든다("지난주",
  "지난 2주"). 월요일 아침에 "최근 2주"로 넓히면 전부 지난주 경기인데 이번 주가 섞인 것처럼
  읽혔다. 목업 설정의 "주간 리포트 알림 · 월요일 오전"과도 맞는다. 4주 동안 한 판도 없으면
  "리포트까지 5경기 남았어요" 대신 "최근 4주 동안 뛴 경기가 없어요"를 띄운다.
- 2026-09-25 · 목업의 큐 칩(경쟁 + 일반 · 경쟁 · 일반 · 기타)을 넣었다. 기타는 `CLAUDE.md`
  규칙대로 K/D와 헤드샷만 보이고 동적 칸이 없다. 4라운드 선취 같은 모드에서는 전투점수와
  피해량이 성립하지 않는다.
- 2026-09-25 · 개발용 키로는 실데이터를 못 붙인다. Riot이 발로란트 API를 열 때 "Personal keys
  and developer keys do not have access to the VALORANT API"라고 공지했다. RSO도 프로덕션 키
  전용이다. 그래서 프로덕션 키가 나올 때까지 가짜 데이터로 화면과 계산을 먼저 끝낸다.
  → 같은 날 직접 호출해 고쳤다. 개발용 키로 VAL-STATUS, VAL-CONTENT, account-v1은 200이고
  VAL-MATCH만 403이다. 경기 데이터가 막혀 있다는 결론은 같다.
- 2026-09-25 · S4 설정을 목업대로 만들었다. 목업과 다른 점은 이렇다.
  - 하단 탭바는 홈과 설정 두 개다. 목업의 경기·친구 탭은 갈 화면이 생기면 붙인다.
  - 알림의 "라이벌 대결 결과" 줄은 뺐다. 라이벌 기능이 아직 없다.
  - "저장된 경기" 줄에는 화살표를 두지 않았다. 목업에는 있지만 갈 곳이 없다.
  - 아바타 자리는 목업의 그라데이션 대신 Riot ID 첫 글자다. 그라데이션은 피하는 것이고, 플레이어
    카드는 콘텐츠 카탈로그가 붙으면 넣는다.
  - 비공식 고지는 목업의 한글 문구가 아니라 Core Policies 영문 원문이다(위 09-25 항목).
  - 줄 높이는 최소 56dp, 본문은 15sp다. 목업의 13.5px로는 터치 영역과 가독성이 모자랐다.
  - 테마와 기본 큐는 바텀시트에서 고르고, 고르면 바로 닫는다. 삭제와 연동 해제는 바텀시트로
    한 번 더 묻는다. 목업에는 누른 뒤 화면이 없다.
  - 연동은 가짜다. 인트로의 시작 버튼이 연동을 대신하고, 연동 해제는 가짜 경기를 비우고 인트로로
    돌아간다. 설정 값만 DataStore(preferences-core 1.2.1)로 기기에 저장한다. 경기 저장소는 아직
    고르지 않았다.
  - 고른 테마는 상태바와 내비게이션 바 아이콘 색에도 적용한다.
- 2026-09-25 · 홈 맨 위의 "오발있" 글자를 ㅇㅂㅇ 로고로 바꿨다. 목업의 그 자리는 로고 자리였다.
  화면 읽기 프로그램에는 "오발있"으로 읽힌다.
- 2026-09-25 · 홈 기간 줄의 역할을 "주로 타격대"로 적는다. 역할만 적으면 그 기간에 그 역할만 한
  것처럼 읽혔다. 기간 중 가장 많은 라운드를 뛴 역할이다.
- 2026-09-25 · S1-a 지표 설명 시트를 목업대로 만들었다. 목업과 다른 점은 이렇다.
  - "어느 정도면 괜찮나요?"(150을 넘기면 제 몫, 프로 평균 160~180)를 "평소에는 어느 정도였나요?"로
    바꿨다. 남의 기준과 비교하면 "비교 대상은 본인의 과거뿐"이라는 선을 넘고, 기준값의 출처도
    없었다. 대신 기간 앞 주들의 내 주간 값 범위를 보여준다. 이번 액트 주가 4주 미만이면 말하지 않는다.
  - 추이 막대는 12주가 아니라 8주다. 첫 수집 범위가 8주다. 액트가 바뀐 곳에 세로선을 긋는다.
  - 라운드가 40에 못 미치는 주는 막대를 비운다. 한두 판짜리 주가 추이를 흔든다.
  - 이번 주 막대의 금색은 목업대로 뒀다. 액센트를 데이터에 쓰지 않는다는 규칙의 예외로 적었다.
  - 설명 문구는 해요체다. 목업은 합니다체였다.
  - 새 색 토큰 `--bar`(막대 면)를 목업 값으로 더했다.
- 2026-09-25 · 고정 4칸과 달라진 점 칸의 간격을 구분선 양옆에만 둔다. 칸 폭에 간격을 넣었더니
  첫 칸만 내용이 넓어 보였다.
- 2026-09-25 · 홈 개선 포인트 문장을 공수 비교로 정했다. 기획에는 문장 규칙이 없었다. 목업
  둘째 줄의 "전략가는 수비에서 오래 버틸수록 라운드가 풀립니다"는 뒷받침할 수 없는 일반론이고
  조언에 가까워서, 숫자와 "전략가에게 생존율은 먼저 보는 지표예요"(역할군 표의 사실)로 바꿨다.
  기준은 10%p · 피해량 15% · 진영마다 40라운드로 시작한다. 라운드에 진영(`mySide`)을 담았고,
  진영을 모르는 라운드는 어느 쪽에도 넣지 않는다. 가짜 경기는 수비에서 첫 교전을 더 자주 지게
  해서 문장이 뜬다.
- 2026-09-25 · 홈 기간 줄의 역할 이름("주로 **타격대**")만 `--t2` SemiBold로 올렸다. 목업은 캡션 전체가
  같은 회색이다. 동적 칸과 개선 포인트를 정하는 값이라 한눈에 들어오게 했고, 색은 쓰지 않았다.
  달라진 점 캡션의 "타격대 기준"은 그대로 둔다. 한 화면에 강조가 두 번이면 강조로 안 읽힌다.
- 2026-09-25 · VAL-CONTENT에 요원 역할과 무기 계열이 없다는 걸 확인했다. 역할은 `characterId`로
  알아낸다는 전제는 그대로지만, ID에서 역할로 가는 표를 VAL-CONTENT에서는 못 받는다. 출처를 정할
  때까지 가짜 데이터가 역할을 직접 담는다.
- 2026-09-25 · 내 프로필을 만들고 S6 무기, S7 요원을 그 아래에 뒀다. 목업에는 두 화면으로 가는 길이
  없었다. 홈 오른쪽 위 아바타(목업의 플레이어 카드 자리)로 들어간다. 목업과 다른 점은 이렇다.
  - 집계 범위는 "최근 50경기"가 아니라 이번 액트다. 액트 경계를 넘는 평균을 만들지 않는다.
  - 설명 문구는 해요체다("…들어와요"). 목업은 합니다체였다.
  - 요원 초상, 무기 이미지, 배너는 그라데이션 대신 면과 이름 첫 글자로 자리를 잡는다.
  - 요원별 승률의 초록·빨강은 목업대로 뒀다. 색 규칙의 예외로 CLAUDE.md에 적었다.
  - 무기의 "요즘 잘 맞아요"는 동적 칸과 같은 1.5배 규칙을 넘을 때만 붙인다. 이번 기간 표본이
    모자라면 이번 액트 값을 대신 보여주고 "이번 액트"라고 적는다.
  - 경기 결과(`myTeamWon`), 요원 이름과 무기 계열은 가짜 데이터가 담는다. VAL-CONTENT에는 역할과
    계열이 없어서 출처를 정해야 한다.
- 2026-09-25 · 친구 탭, S5 친구 프로필, 홈의 라이벌·친구 비교를 가짜 데이터로 만들었다.
  - 친구는 서로 수락하는 방식으로 뒀다. 기기에만 저장하는 한쪽 팔로우도 검토했지만, Riot 정책은
    데이터 공유에 동의하지 않은 사람의 기록을 남에게 보여주지 못하게 하고, 기기만으로는 상대가
    동의했는지 알 수 없다. 신청서에도 서로 수락한 친구끼리만 본다고 적었다.
  - "비교 대상은 본인의 과거뿐"에 서로 수락한 친구를 예외로 적었다. 목업 홈에 라이벌 대결과
    친구 순위가 있고, 앱 전체나 모르는 사람과는 여전히 비교하지 않는다.
  - 친구 탭은 목업에 없어서 설정과 같은 짜임으로 그렸다. 닉네임 검색은 두지 않았다.
  - S5의 "최근 경기"는 뺐다. 맵 이름과 스코어가 모델에 없다.
  - S5 "나와 비교"는 내 리포트 기간으로 센다. 친구 칸은 친구 자신의 기간이라, 비교 캡션에
    기간을 같이 적었다("이번 주 · 나 · 민석").
  - 탭바는 홈 · 친구 · 설정 세 개다.
- 2026-09-25 · 지표 표기(`MetricFormat`, 고정 지표 이름, 기간 이름)를 `feature/report`에서 새
  모듈 `core/ui`로 옮겼다. S5에도 같은 4칸이 들어가서, 반올림 규칙을 두 군데에 두지 않으려고 했다.
- 2026-09-25 · 경기 모델에 맵, 경기 길이, 라운드별 승패, 스코어보드, 라운드가 끝난 방식, 장비 가치를
  넣었다. S2 경기 목록과 S3 경기 상세, 이코노미 지표에 필요하다.
  - 라운드별 승패(`roundOutcomes`)는 내가 튕겨서 못 뛴 라운드까지 담는다. 지표는 계속 내가 뛴
    라운드(`rounds`)로만 센다.
  - 스코어보드 숫자는 응답의 `players[].stats`를 그대로 옮긴다. 게임 스코어보드와 같아야 해서다.
    그래서 내 줄의 킬 수가 스킬 자살과 팀킬을 뺀 리포트 K/D의 킬 수와 다를 수 있다.
  - 지금 티어는 가장 최근 경쟁전의 `competitiveTier`로 읽는다. 응답은 경기 당시 티어만 준다.
- 2026-09-25 · 동적 칸 후보에 이코·포스바이·풀바이 승률을 넣었다. 목업 홈의 포스바이 승률 칸과
  S0-4의 "라운드 운영 다듬기"가 이 지표를 쓴다. 우리 팀 한 사람당 평균 장비 가치로 가르고(2,000 미만
  이코, 3,900 이상 풀바이), 피스톨 라운드는 뺀다. 최소 표본은 15·15·40라운드다. 둘 다 공식 기준이
  없어 시작 기준선이다.
- 2026-09-25 · 동적 칸 순서에 S0-4 관심사를 넣었다. 역할 우선 지표 다음, 변화량 앞이다. 역할이 크게
  띄우지 않는 지표는 관심사여도 넣지 않는다.
- 2026-09-25 · 가짜 데이터의 요원, 맵, 무기 ID를 콘텐츠 카탈로그의 실제 UUID로 바꿨다. 번들한 이미지가
  그대로 붙는다. 스코어는 13승 선취로, 경쟁전 연장은 두 라운드 차이로 끝나게 했다.
- 2026-09-25 · 콘텐츠 카탈로그(13.06)를 받아 이미지를 붙였다. 이니셜로 자리만 잡던 요원, 무기, 맵,
  역할 자리에 실제 그림이 들어간다.
  - 서버가 카탈로그를 올려 두고 ID로 내려주는 것을 기본으로, 앱에 넣은 이미지는 대체용으로 둔다.
    op.gg가 `c-valorant-api.op.gg/Assets/PlayerCards/{UUID}_small.png`처럼 카탈로그 파일을 그대로 쓰는
    것을 확인했다.
  - 파일 이름은 영문 이름으로 짓고 UUID에서 파일로 가는 표를 스크립트가 만든다. UUID 파일 이름은 사람이
    알아볼 수 없어서 바꿨다.
  - 무기는 기본 스킨 그림을 쓴다. 무기 폴더의 흰 선화를 글자색으로 칠해 봤지만 op.gg처럼 실제 총
    모양이 나오지 않았다. 짙은 회색 총이 다크 테마 바탕에 묻혀서 목업의 무기 칸처럼 `--fill` 면 위에
    올렸다. 목업의 칸(32×19)에 넣으면 총이 너무 작아서 목록은 44×24, 위쪽 두 줄은 52×30으로 키웠다.
  - 쓰지 않는 것은 뺐다. 모드 전용 무기와 스킬 장비, 쓰이지 않는 케이/오 ID, 사격장·기초 훈련·난투 맵.
  - 앱에 넣는 이미지는 약 2.6MB다. 맵 배너 1.2MB가 가장 크다.
  - 아바타는 op.gg처럼 가장 최근 경기에 고른 요원 얼굴로 그린다. 플레이어 카드는 경기 응답의
    `players[].playerCard`로 알 수 있지만 1,016장이라 앱에 넣지 않았다. 서버가 생기면 프로필 배너에만
    붙인다. 가짜 데이터용으로 넣었던 카드 26장도 앱 용량만 늘려서 지웠다. 요원을 모르면 기본 이미지 없이
    Riot ID 첫 글자를 띄운다.
- 2026-09-25 · 티어 이름과 엠블럼, 요원 역할표를 valorant-api.com에서 받기로 했다. Riot 공식 자료
  (VAL-CONTENT, 카탈로그, 개발자 문서)에는 셋 다 없다. tracker.gg, op.gg, dak.gg, Blitz가 모두 게임
  파일에서 뽑은 엠블럼을 자기 서버에 올려 쓰는 것을 확인하고 같은 방식으로 간다. 공식 에셋이 아니라는
  위험은 남는다.
- 2026-09-25 · 홈 오른쪽 위에 목업대로 티어 엠블럼과 이름을 아바타 옆에 붙였다. 내 프로필과 S5 머리에도
  목업 S5처럼 이름 옆 엠블럼, "다이아몬드 2 · 주로 타격대" 줄을 뒀다. 줄이 넘치면 점은 같은 줄 항목
  사이에만 찍는다.
- 2026-09-25 · 누름 효과를 물결로 통일했다. MaterialTheme을 쓰지 않아 `clickable`만 단 줄과 아바타에
  Compose 기본값인 회색 사각형이 깔리고 있었다. 테마에서 기본값을 물결로 바꾸고, 아이콘은 원, 작은 버튼은
  버튼 모양 안에서만 퍼지게 했다.
- 2026-09-25 · 무기 계열을 접고 펼 때와 받은 요청이 줄어들 때 높이가 부드럽게 바뀌게 했다.
- 2026-09-25 · 홈 고정 칸 라벨은 7sp까지 줄어들게 했다. 화살표를 붙인 뒤로 작은 화면에서 글자를 키우면
  9sp로도 "전투점수"가 "전투"로 잘렸다.
- 2026-09-25 · 경기 탭(S2 경기 목록, S3 경기 상세)을 만들고 탭바를 목업대로 네 개로 늘렸다. 목업과 다른 점은
  이렇다.
  - S2 오른쪽 위 필터 아이콘은 목업에 동작이 없어서 요원과 맵을 고르는 시트로 정했다.
  - S3의 라운드·이코노미 탭은 목업에 이름만 있어서 내용을 정했다. 라운드마다 진영, 끝난 방식, 내 킬과
    퍼블·퍼데를, 이코노미는 유형별 승리와 라운드별 장비 가치를 적는다.
  - 스코어보드는 게임처럼 전투 점수 순으로 세운다. 목업은 내 줄이 맨 위였다.
  - 목업 문구 "앱을 쓰는 플레이어의 프로필만 열 수 있어요."를 "친구의 프로필만 열 수 있어요. 다른
    플레이어를 누르면 친구 요청을 보낼 수 있어요."로 바꿨다. 프로필은 서로 수락한 친구만 본다. 친구가 아닌
    사람을 누르면 시트로 안내하고 친구 요청이나 초대 링크를 보내게 했다(사용자 결정).
  - 배너 위아래에 흐림막을 깔았다. 라이트 테마에서 맵 그림 위 캡션이 안 읽혔다.
  - 글자를 키우면 경기 줄의 두 칸이 폭을 반씩 나누고, 넘치는 말은 점 없이 다음 줄로 내린다. 스코어보드
    숫자 열은 글자 크기를 따라 넓히고 이름은 줄어들다 말줄임표로 끝난다.
- 2026-09-25 · S5에 목업의 "최근 경기"를 붙였다. 세 판을 두고 "전체 보기"로 친구의 경기 목록을 연다.
  줄은 눌러도 열리지 않는다. 친구 경기의 스코어보드에는 내가 안 뛴 경기의 다른 사람 기록이 있는데, 앱을
  안 쓰는 사람의 기록은 내가 뛴 경기 안에서만 보여줄 수 있다. 날짜 머리가 없어서 시각은 "2시간 전",
  "어제", "9/22"로 적는다.
- 2026-09-25 · `OvalitTheme` 안에 MaterialTheme을 같이 깔았다. 우리 색·글꼴 토큰을 Material 색 이름에 옮겨
  담아서, 바텀시트·물결·글자 선택처럼 Material 컴포넌트가 스스로 고르는 값이 우리 것과 맞는다. 처음에
  MaterialTheme을 뺀 것은 Material 색 이름에 맞춰 화면을 짜면 배경 밝기로 영역을 나누는 규칙이 흐려져서였고,
  화면 코드는 계속 `OvalitTheme.colors`만 쓴다(사용자 결정).
- 2026-09-25 · S0-2 연동 동의와 S0-4 불러오는 중을 만들었다. 인트로 → S0-2 → (S0-3은 RSO가 붙기 전까지
  건너뜀) → S0-4 → 홈 순서다. 목업과 다른 점은 이렇다.
  - S0-4 관심사 설명에서 아직 계산하지 않는 항목(스킬 사용, 스파이크 설치, 경기마다 들쑥날쑥한 정도)을
    뺐다. 고른 뒤 아무것도 달라지지 않는 말을 적어 두지 않으려고 했다.
  - 다 받으면 아래 문구 자리에 "리포트 보기" 버튼을 둔다. 목업은 받는 도중만 그렸다.
  - 막대는 목업처럼 몇 칸이 아니라 받을 경기 수만큼 칸을 나눈다. 칸마다 그 경기의 승패 색이다.
  - 관심사는 설정의 표시 묶음에서도 바꾼다. 목업 설정에는 그 줄이 없지만 S0-4가 "나중에 설정에서 바꿔도
    돼요"라고 약속한다.
- 2026-09-25 · 첫 수집을 WorkManager에 맡겼다. CLAUDE.md는 WorkManager 동기화 계층을 버렸는데, 목업 S0-4가
  "앱을 닫아도 계속 분석해요. 다 모으면 알림으로 알려드릴게요."라고 한다. 주기 동기화는 여전히 두지 않고
  첫 수집 한 번만 맡긴다(사용자 결정). 급한 작업(expedited)으로 걸지 않았다. 안드로이드 12 아래에서는
  포그라운드 서비스 알림을 따로 띄워야 해서다.
- 2026-09-25 · 아바타에서 요원 얼굴을 뺐다. "op.gg처럼 요원 얼굴로"는 경기 기록에 그 판의 요원을 넣으라는
  뜻이었는데 아바타로 잘못 옮겼다. 홈 오른쪽 위, 프로필 머리, 친구 목록의 아바타는 플레이어 카드를 서버에서
  받기 전까지 Riot ID 첫 글자다. 스코어보드와 경기 줄은 그대로 요원 얼굴이다(사용자 결정).
- 2026-09-25 · 프로필 머리의 역할 아이콘을 "주로" 뒤, 역할 이름 앞으로 옮겼다. 목업은 "[아이콘] 주로
  타격대"인데 아이콘이 "주로"까지 꾸미는 것처럼 읽힌다. 줄이 넘어가도 아이콘과 이름이 떨어지지 않게 글자
  안에 넣었다(사용자 결정).
- 2026-09-25 · 내 프로필을 op.gg식 요약으로 넓혔다. 첫 안(이번 액트 요약, 티어 흐름, 요원·무기 그림, 최근
  경기)과 op.gg 프로필을 합친 안이다(사용자 결정). op.gg에서 뺀 것은 이렇다.
  - 평점(KDA 비율)과 PS. PS는 op.gg가 만든 종합 점수이고, 평점은 비율 하나지만 이름이 실력 점수처럼 읽힌다.
  - K/D 같은 숫자를 좋고 나쁨으로 칠하는 것. 색은 승률과 경기 결과에만 쓴다. 경쟁전 승률은 요원별 승률과
    같은 규칙으로 칠한다.
  - 몸 그림. op.gg가 직접 그린 일러스트라 머리·몸·다리 막대로 바꿨다.
  - 지난 액트 티어. 첫 수집이 8주라 지난 액트의 마지막 티어를 확실히 알 수 없다.
  - 역할군 줄. S7에 있고 요원 칸 제목에 "타격대 78%"가 있다.
- 2026-09-25 · 프로필 머리가 상태 표시줄 밑까지 깔려 있어서 내리면 글자가 시계와 겹쳤다. 배너가 그 자리를
  벗어나면 바탕색으로 덮는다. S5도 같은 머리라 같이 고쳤다.
- 2026-09-25 · 내 프로필의 티어 흐름 그래프를 뺐다. 사용자가 원한 것은 op.gg식 목업 그대로였다(사용자 결정).
  위쪽을 그 목업에 맞췄다. 티어는 면을 깐 카드 하나이고, 통계는 피해량, K/D, 전투점수, 최다 킬, 판당 K/D/A,
  플레이 시간 순서이며, 맞힌 부위는 "24% 442"처럼 한 줄이다. 요원·무기 요약과 최근 경기는 합친 안대로 남겼다.
- 2026-09-25 · 백엔드를 Cloudflare Workers 무료 플랜과 D1로 정했다. 카드 없이 쓰고 한도를 넘으면 막힌다. KV는
  하루 쓰기 1,000번이라, R2는 카드 등록이 필요해서 뺐다. 계정이 생기기 전까지 로컬에서만 돌린다.
  - 세션은 32바이트 불투명 토큰이고 90일 뒤 끝난다. 연장하지 않는다. 로그인 코드는 2분 일회용, RSO state는
    10분이다. verifier가 틀려도 로그인 코드를 지운다. 가로챈 코드로 여러 번 시도할 수 없는 대신, 그때 정상
    사용자는 다시 로그인해야 한다.
  - RSO 콜백이 실패하면 JSON 대신 `ovalit://auth?error=`로 돌려보내 앱이 Custom Tabs를 닫고 안내하게 한다.
  - `/auth/dev`는 환경 변수와 로컬 주소 두 겹으로 막았다. 아무 PUUID로나 들어올 수 있어서다.
  - 내가 안 뛴 친구 경기는 친구와 나 말고 모두 가린다. 응답 모양을 다 몰라 PUUID 모양 문자열은 어디 있든
    바꾼다. 파티 ID는 여러 경기에 걸쳐 같아서 남기면 다른 경기의 같은 사람을 찾을 수 있어 같이 바꾼다.
  - 상대가 먼저 요청했으면 새 요청 대신 수락을 누르게 한다(409). 초대 링크는 7일 동안 여럿이 쓰고, 쓰면 초대한
    사람에게 요청이 가서 수락해야 친구가 된다.
  - Cache API가 `*.workers.dev`에서 아무것도 담지 않는다. 사용자 정의 도메인은 돈이 들어서, isolate 메모리
    캐시와 D1 `match_players`로 대신한다. 새 경기마다 약 10행을 써서 하루 1만 경기쯤까지 무료 한도 안이다.
    같은 이유로 VAL-CONTENT와 점검 안내를 세션 뒤로 옮겼다. 남이 되풀이해 불러 레이트 리밋을 쓰지 못하게 한다.
  - 빌드·테스트 도구는 배포물에 들어가지 않아 라이선스 제한(Apache-2.0, MIT, OFL)을 두지 않는다. wrangler가
    sharp(LGPL-3.0)를, vite가 lightningcss(MPL-2.0)를 끌어온다. Worker 번들에 들어가는 것은 hono(MIT)뿐이다.
  - wrangler 사용 통계는 보내지 않게 설정했다.
- 2026-09-25 · 홈에 친구·라이벌 유도 칸을 넣었다. 목업에는 없는 칸이다. 라이벌이 없으면 홈이 개선 포인트에서
  끝난 것처럼 보여서, 리포트·친구·라이벌 순서로 따져 하나만 권한다(사용자 결정). 라이벌은 홈의 시트에서 바로
  고르게 했다. S5까지 가서 고르게 하면 한 번 더 떠나야 한다.
- 2026-09-25 · 하단 탭에서 고른 탭을 채운 아이콘으로 바꿨다. 목업은 색만 바꾸는데 선이 가늘어 어느 탭인지 한눈에
  안 들어온다. 토스, 당근, 배민이 모두 이렇게 한다. 목업의 경기 아이콘(세 줄)은 메뉴 버튼처럼 읽혀서 경기 기록
  한 장을 뜻하는 카드 모양으로 바꿨다(사용자 결정).
- 2026-09-25 · 다크 테마의 무기 받침을 `--fill`에서 `--t5`로 한 단계 밝혔다. 기본 스킨 총이 짙은 회색이라
  `--fill` 위에서는 모양이 안 보였다. 라이트는 그대로 `--fill`이다.
- 2026-09-25 · 홈 "달라진 점" 세 칸을 옆으로 밀지 않고 화면 폭에 나눴다. 목업은 세 번째 칸을 화면 끝에서 잘라
  밀 수 있다는 걸 보이는데, 숫자가 "+4"에서 끊겨 깨진 화면처럼 보였다(사용자 지적). 좁은 칸에서는 변화량을 숫자
  아래로 내리고, 칸 이름과 설명은 한 줄로 두되 넘치면 같은 비율로 줄인다.
- 2026-09-25 · 누름 효과를 물결에서 누르는 즉시 까는 옅은 면과 살짝 줄어드는 크기로 바꿨다. 물결은 누르고 있어도
  한참 뒤에야 퍼져서 앱이 늦게 반응하는 것처럼 보였다(사용자 지적). 토스와 당근처럼 누른 것이 바로 어두워지고
  조금 작아지게 했다(사용자 결정). 목록 안에서는 스크롤과 구분하려고 Compose가 누름을 약 0.1초 늦게 알리는데,
  이건 안드로이드 기본 동작이라 그대로 둔다.
- 2026-09-26 · 나란한 칸의 글자를 칸마다 따로 줄이지 않고 한 크기로 맞췄다. 320dp 화면에 글자 1.5배에서 "전투점수"만
  작아지고 그 칸 숫자만 위로 올라가 줄이 어긋났다. 내 프로필 통계는 세 칸에 "17.0/12.1/6.1"이 안 들어가 잘려서
  그럴 때는 두 칸씩 놓는다.
- 2026-09-26 · 역할 표기를 "주로 타격대"에서 "타격대 78%"로 바꿨다(사용자 결정). "주로"는 말을 줄인 듯이 읽히고,
  빼면 그 역할만 한 것처럼 읽힌다. 비중은 역할을 아는 라운드 중 그 역할로 뛴 라운드다. 내 프로필은 머리에 비중이
  생겨서 요원 칸 제목 줄의 같은 숫자를 뺐다.
- 2026-09-26 · 무기 그림의 받침 면을 없애고 기본 스킨 그림을 `--t2` 한 가지로 칠한 실루엣으로 바꿨다. `--t5` 받침이
  칸마다 탁한 갈색 상자처럼 떠 보였다(사용자 지적). 실루엣은 두 테마 모두 면 없이 보인다. 내 프로필의 두 무기는 반씩
  나누던 것을 요원과 같은 세 칸 격자에 놓았다. 반씩 나누면 요원과 줄이 안 맞고 가운데가 빈다.
- 2026-09-26 · S7 요원 표의 두 지표 열을 고를 수 있게 했다(사용자 요청). 목업은 주 역할에 맞춘 열만 둔다. 주 역할
  묶음으로 시작하고 표 위 버튼으로 관여율 · 생존율, 퍼블 관여율 · 첫 교전 승률, K/D · 피해량, 전투점수 · 헤드샷 중에
  고른다. 본인 요원끼리의 비교라 지켜야 할 선에 닿지 않는다.
- 2026-09-26 · 누른 면의 모양을 네모와 원에서 모서리 12dp 둥근 사각형으로 바꿨다(사용자 결정). 토스처럼 줄과
  아이콘 버튼 모두 둥근 사각형으로 깐다. 탭바는 칸이 가로로 길어 아이콘과 이름을 감싸는 64×52dp만 깐다.
- 2026-09-26 · S6 무기 표에 K/D/A와 라운드당 피해량을 더했다(사용자 요청). 목업은 킬과 헤드샷뿐이다. 죽을 때 든 무기와
  피해를 준 무기는 응답에 없어서 데스·어시스트·피해량은 그 무기를 들고 시작한 라운드(`economy.weapon`)로 센다. 킬은
  지금처럼 `finishingDamage`다. 기준이 섞이니 표 밑에 적는다.
- 2026-09-26 · 위쪽 주력 무기를 둘에서 셋으로 늘렸다(사용자 요청). 내 프로필은 요원과 같은 세 칸 격자라 빈칸이 없어진다.
- 2026-09-26 · 에이스와 클러치를 넣었다(사용자 요청). S3 라운드 탭에 "에이스", "1대3 클러치"를 적고 내 프로필 통계에
  에이스 횟수와 클러치("4번 중 2번")를 둔다. 기획은 클러치를 감시자 우선 지표로만 적어 뒀고 에이스는 없었다. 진 클러치를
  라운드마다 적으면 진 라운드에 꼬리표가 붙어서 이긴 것만 적는다.
- 2026-09-26 · 목업 서체 IBM Plex Sans KR로 바꿔 봤다가 Pretendard로 남겼다(사용자 결정). Plex는 SIL OFL 1.1(예약 이름
  "Plex")이라 고치지 않은 채 앱에 넣는 건 되고, 한글 11,172자를 다 담는다. 다만 가변 폰트가 없어 400~700 네 굵기에
  11.4MB로 Pretendard 한 벌(6.7MB)보다 무겁다. `tnum` 기능은 없고 숫자가 처음부터 고정폭(600)이다. 목업이 더 개성 있어
  보인 건 한글보다 숫자에 쓴 Archivo 쪽이다.
- 2026-09-26 · S6 위쪽 세 무기에 K/D와 라운드당 피해량을 더해 헤드샷과 나란한 세 열 표로 바꿨다(사용자 요청). 목업은
  헤드샷 하나에 "4주 평균 21%"만 둔다. 헤드샷 숫자에 이름이 없어 내 프로필의 "킬 34%"와 같은 지표로 읽혔고, 무기를 잘
  쓰는지 보려면 K/D와 피해량도 있어야 했다. 세 지표 모두 이번 기간을 그 앞 4주 평균과 견주고, 변화량은 지표마다 평소
  변동폭 규칙을 넘을 때만 칠한다. 기간 표본이 하나라도 모자라면 그 줄은 통째로 이번 액트 값이다.
- 2026-09-26 · 위쪽 세 무기는 킬 순서로 둔다(사용자 결정). 많이 들고 시작한 라운드 순으로 바꿔 봤다가 되돌렸다. 판 수로
  세면 피스톨 라운드 때문에 권총이 거의 모든 판에 든다. 이름 밑 킬은 이번 액트 킬이다. 이번 주를 띄운 줄에 이번 주
  킬을 적었더니 킬이 더 많은 팬텀이 킬이 적은데 1위인 것처럼 보였다(사용자 지적).
- 2026-09-26 · 프로필 배너를 고정 112dp에서 상태 표시줄 높이 + 104dp로 바꿨다. 상태 표시줄이 40dp인 실기기에서 머리
  줄이 내려오면서 아바타가 뒤로 가기 버튼을 덮었다(사용자 지적). 아바타는 머리 줄보다 8dp 아래에서 시작한다.
- 2026-09-26 · 홈 동적 칸을 3개에서 3~5개로 늘리고 관심사 지표를 맨 앞에 늘 두게 했다(사용자 결정). 목업과 기획은
  세 칸이고, 관심사는 역할 우선 지표 다음 순위였다. 그래서 다른 지표가 셋 넘게 움직인 주에는 고른 관심사가 홈에서
  빠졌다. 라운드 운영은 지표가 셋이라 세 칸이면 다른 변화가 들어갈 자리가 없다. 관심사 지표는 역할이 크게
  띄우지 않는 지표여도 넣는다. 전략가가 에임 올리기를 골랐는데 첫 교전 승률이 안 뜨던 걸 이렇게 푼다. 한 줄에 세
  칸씩 놓고 넘치면 다음 줄로 넘긴다. 목업처럼 옆으로 밀면 칸이 화면 끝에서 잘린다.
- 2026-09-26 · K/D/A 옆에 K/D 비율을 같이 적게 했다(사용자 요청). 내 프로필은 K/D 칸 밑에 판당 K/D/A를 붙이고 따로
  있던 판당 K/D/A 칸을 없앴다. 플레이 시간은 제목 옆으로 옮겨 여섯 칸이 세 칸씩 두 줄로 맞는다. S6 계열 표는 오른쪽에
  K/D 열을 더해 위쪽 세 무기 표와 열을 맞췄다. 사용자 예시는 "1.24(10/11/2)"였지만 비율은 K/D만 쓴다. (킬 + 어시) ÷
  데스인 KDA 비율은 킬과 어시를 더한 점수라 신청서의 "지표는 따로 보여준다"와 부딪힌다.
- 2026-09-26 · 바로 앞 결정을 바꿔 KDA((킬 + 어시) ÷ 데스)를 쓰기로 했다(사용자 결정). 신청서에 "Metrics are always
  shown individually"와 종합 점수 없음을 적었고 KDA는 킬과 어시를 더한 값이라 걸릴 수 있다고 알렸다. 사용자는 op.gg식
  "평점" 대신 "KDA"라는 이름을 골랐다. K/D/A 합계 옆에만 두고 줄 세우기, 친구 비교, 색, 변화량에는 쓰지 않는다. 내
  프로필은 K/D 칸과 KDA 칸을 따로 두고 KDA 밑에 판당 K/D/A를 붙이며, 플레이 시간은 다시 칸으로 돌렸다. S6 계열 표는
  이름 밑을 "1.87 (369/267/131)"로 바꾸고 K/D 열은 그대로 둔다.
- 2026-09-26 · 개선 포인트 문장도 관심사 지표를 역할 우선 지표보다 먼저 본다(사용자 요청). 동적 칸과 같은 까닭이다.
  라운드 운영 다듬기는 원래 후보에 없던 공수별 이코·포스바이·풀바이 승률로 보고, 진영마다 표본이 모자라면 역할 기준으로
  돌아간다. 관심사가 없는 사람의 문장은 바뀌지 않게 세 승률은 라운드 운영을 골랐을 때만 후보다.
- 2026-09-26 · 내 프로필 무기 칸을 "킬 43%"에서 킬 수, 헤드샷, 라운드당 피해량으로 바꿨다(사용자 요청). 비중보다 그
  무기를 잘 쓰는지가 보이는 숫자가 낫다. 요원 칸에는 승률 밑에 "KDA 1.83"만 더했다(합계는 빼 달라는 사용자 결정). 홈
  고정 칸 밑 줄에도 판당 K/D/A 옆에 KDA를 붙였다. 이 칸들은 S5와 같이 쓰려고 `core/ui`로 옮겼다.
- 2026-09-26 · S5 친구 프로필을 내 프로필처럼 넓혔다(사용자 결정). 목업 S5는 같이 한 경기, 이번 주 4칸, 나와 비교, 최근
  경기뿐이었다. 순서는 같이 한 경기, 티어 카드, 통계, 나와 비교, 맞힌 부위, 요원, 무기, 최근 경기이고 이번 주 4칸은
  뺐다. 신청서는 친구 프로필을 "Showing player stats"로 적었고 서로 수락한 친구끼리만 보인다는 것 말고는 보여줄 항목을
  제한하지 않았다. 심사자가 본 목업과는 달라진다. 전적 비공개 친구는 그대로 같이 한 경기만 본다.
- 2026-09-26 · 홈에 기간 승패와 "이번 주 요원·무기" 칸을 넣었다(사용자 요청). 목업 S1에는 승률도 요원·무기 요약도
  없다. 승패는 경기 결과라 선에 걸리지 않고 색도 경기 결과 규칙을 따른다. 요원·무기 칸은 누르면 S7과 S6으로 가는데,
  그 화면은 이번 액트 전체라 칸 제목에 기간을 붙였다. 한 주에 5판을 넘기는 요원이 드물어서 요원 칸은 판 수 대신 승패를
  적는다.
- 2026-09-26 · S7 요원 표의 줄마다 이름 밑에 KDA를 더했다(사용자 요청). 열로 두면 승률과 고르는 두 열까지 네 열이 되어
  좁은 화면에서 숫자가 붙는다. 그래서 역할과 판 수 뒤에 붙인다. 5판에 못 미친 요원은 다른 숫자처럼 적지 않는다.
- 2026-09-26 · 경기를 새로 받는 규칙을 정했다(사용자 결정). 내 경기는 첫 수집 뒤로 새로 끝난 경기만 받는다. 친구 경기도
  기기에 저장해 S5를 열면 바로 보여주고, 마지막으로 받은 지 10분이 지났을 때만 새 경기를 받는다. 직접 새로 받는 건 같은
  친구에 1분에 한 번이다. op.gg처럼 "마지막 갱신"을 둔다. 레이트 리밋이 앱 전체에 걸리고 무료 서버 한도(요청 하루 10만,
  한 요청의 추가 호출 50개)도 있어서다. 지금 앱은 가짜 저장소라 실제 적용은 프로덕션 키를 받은 뒤다.
