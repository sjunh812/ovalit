# 오발있

발로란트 전적 진단 앱. 최근 경기에서 지표가 어떻게 움직였는지 보여주고, 그 주에 무엇이
달라졌는지 짚어준다.

기획 전문과 화면 목업은 저장소 밖 claude.ai 아티팩트에 있다. 이 파일에는 코드를 쓸 때 매번 필요한 것만 둔다.

- 화면 목업: [발로란트 전적 진단 앱 — 화면](https://claude.ai/artifact/8bAN3dfUBywjmw43Y1euVv). 프로덕션 키 심사에 낸
  것과 같은 화면이다(https://ovalit.netlify.app/).
- 기획 정리: https://claude.ai/artifact/9uEr7ynVcSTSDLBZFH5LKW
- 프로덕션 키 신청서: https://claude.ai/artifact/RVCJYCfD4BF4W6Q2seXKF8 (2026-09-19 제출)

세 문서는 심사에 낸 원본이라 고치지 않는다. 달라진 결정은 이 저장소에만 적는다. 화면을 만들거나 고치기
전에 목업의 그 화면부터 본다. 심사자는 신청서와 목업을 나란히 놓고 보므로 앱이 목업에서
벗어나면 안 된다. 벗어나야 하면 먼저 묻고 `docs/DECISIONS.md`에 남긴다.
글과 커밋 규칙은 `docs/CONVENTIONS.md`에 있다.

## 문서 동기화

이 저장소가 구현의 기준이다. 기획서와 화면 목업은 Claude 쪽 문서에 있어 여기서 고칠 수
없다. 개발하다 결정이 바뀌면 이 저장소의 문서를 먼저 고친다.

- 지켜야 할 선, 지표 규칙, 역할군, 용어, 색 토큰, 화면 구조 중 하나라도 바뀌면
  **같은 커밋에서** `CLAUDE.md`를 고친다.
- 기획서와 달라진 결정은 `docs/DECISIONS.md`에 한 줄 남긴다. 날짜, 무엇이 바뀌었는지,
  왜 바뀌었는지.
- 코드가 `CLAUDE.md`와 어긋나면 코드부터 고치지 않는다. 문서가 맞으면 코드를 고치고,
  코드가 맞으면 문서를 고친다. 어느 쪽이든 어긋난 채로 두지 않는다.

문서를 안 고치면 다음 세션이 옛 규칙을 읽고 되돌려놓는다. 이 규칙은 그래서 있다.

## 지켜야 할 선

Riot 프로덕션 키 승인 조건이다. 어기면 키가 회수된다. 기능을 제안하기 전에 여기부터 본다.

- 여러 지표를 가중합한 종합 점수를 만들지 않는다. MMR을 대신하는 점수로 읽힌다.
- 비교 대상은 본인의 과거뿐이다. 우리가 만든 분포에서 몇 등인지도 같은 이유로 안 된다.
  예외는 서로 수락한 친구다. 홈의 라이벌 대결과 친구 비교는 그 친구들끼리만 줄을 세운다.
- 닉네임 검색이 없다. 친구는 같이 뛴 경기의 스코어보드나 초대 링크로만 추가한다.
- 프로필은 서로 수락한 친구끼리만 본다.
- 앱을 안 쓰는 사람은 내가 뛴 경기 안의 기록까지만 보여준다. 다른 경기, 통산 승률,
  티어 추이는 안 된다.
- 상점 조회, 스카우팅, 인게임 오버레이, 닉변 이력은 없다.
- 게임 결정을 대신하지 않는다. "밴달 쓰세요"는 안 되고 "3주 전부터 팬텀 비중이
  늘었습니다"는 된다.
- RSO 인증 전에는 어떤 전적도 요청하지 않는다.
- Riot 비공식 앱이라는 고지를 인트로와 S4 설정 하단에 둔다. 개발자 포털 정책은 플레이어가
  언제든 찾을 수 있는 자리를 요구한다. 한 번 보여주고 사라지면 안 되고, 모든 화면에 둘 필요는
  없다. 문구는 개발자 포털 General Policies의 Core Policies가 지정한 영문 원문 그대로 쓴다.
  번역하거나 줄이지 않는다. "Legal Jibber Jabber" 문구는 팬 프로젝트용이라 API를 쓰는 이 앱의
  것이 아니다.

전투점수(ACS)와 티어는 예외다. 게임이 스코어보드에 이미 띄우는 공식 값이라 그대로 써도 된다.

KDA((킬 + 어시) ÷ 데스)는 사용자 결정으로 K/D/A 합계 옆에만 "KDA"라는 이름으로 쓴다(2026-09-26). 킬과 어시를 더한
값이라 신청서의 "Metrics are always shown individually"에 걸릴 수 있다. 그래서 "평점"이라 부르지 않고, 줄을 세우거나
친구와 견주는 데 쓰지 않고, 색을 칠하거나 변화량을 붙이지도 않는다.

## 아키텍처

CMP(Compose Multiplatform)로 쓰고 출시는 안드로이드만 한다. iOS는 시뮬레이터에서 도는
데까지다. TestFlight 외부 테스트에 Beta App Review가 걸려 배포할 길이 없다.

DI는 Koin 4를 쓴다. Hilt는 KMP를 지원하지 않는다. 회사 코드와 다르니 주의한다.

백엔드가 필요하다. RSO가 Authorization Code 방식이라 `client_secret` 교환이 서버에서
일어나야 하고, RGAPI 키도 앱에 넣을 수 없다. 앱에는 자체 세션 토큰만 내려준다.

RSO는 Custom Tabs로 띄운다. WebView는 쓰지 않는다. 앱이 비밀번호를 만지지 않는다고
신청서에 적었다.

끝난 경기는 결과가 바뀌지 않는다. 한 번 받아 저장하면 다시 요청하지 않는다. 레이트 리밋도
이렇게 아낀다. Riot 레이트 리밋은 사용자마다가 아니라 앱 전체에 걸리니, 한 사람이 화면을 여는 만큼 모두의 몫이 준다.

- 내 경기: 첫 수집(S0-4)만 최근 50경기를 받는다. 그 뒤 홈과 경기 탭을 당기면 경기 ID 목록을 한 번 받고, 저장해 둔
  것과 비교해 새로 끝난 경기만 받는다(`MatchRepository.refresh`). 오래 쉬었다 와서 받을 경기가 여러 개면 몇 경기를
  받는 중인지 보여준다.
- 친구 경기: 받은 경기는 내 경기처럼 기기에 저장한다. S5를 열면 저장된 것을 바로 보여주고, 마지막으로 받은 지
  10분이 지났을 때만 뒤에서 새 경기만 받는다. 열 때마다 받지 않는다. S5에 "마지막 갱신 3분 전"을 적고, 당겨서 새로
  받는 건 같은 친구에 1분에 한 번까지다. 10분과 1분은 시작 기준선이다.
- 서버: 경기 ID 목록은 새 경기가 바로 보여야 해서 담아 두지 않고, 경기 상세는 30일 담아 둔다. 다만 isolate 메모리와
  Cache API는 언제든 비므로 거기에 기대지 않고 앱이 저장한다.

지표 표기(보이는 자릿수 반올림, 고정 지표 이름, 기간 이름, 라이벌 대결 줄), 역할 이름, 게임 이미지,
프로필 머리와 프로필 칸(티어 카드, 통계, 맞힌 부위, 요원, 무기), KDA 줄은 `core/ui`에 두고 기능 화면이 같이 쓴다.
반올림 규칙이나 이름을 기능마다 따로 두면 한쪽만 고치다 어긋난다.

nowinandroid에서 가져올 것: UDF, `sealed interface UiState`,
`stateIn(WhileSubscribed(5_000))`, Repository 분리, `build-logic` convention plugin.

화면 전환은 Navigation 3이고 `composeApp`에만 둔다. 출시가 안드로이드뿐이라 안드로이드
아티팩트를 쓴다. 기능 모듈은 `ReportRoute`처럼 ViewModel을 받아 그리는 진입 컴포저블까지만
내놓고, 어디서 어디로 가는지는 `composeApp`이 정한다.

버릴 것: SyncWorker 주기 동기화 계층, nowinandroid의 FTS 전문 검색, 20여 개 모듈 구조.
WorkManager는 S0-4 첫 수집 한 번만 맡긴다. 앱을 닫아도 이어 받고 끝나면 알림을 보낸다. 공유 모듈은
`ImportScheduler`만 알고, WorkManager 구현은 `composeApp`에 둔다. 로컬 DB는 아직 안 고른다. `core/data`에는 Repository 인터페이스와
가짜 구현만 두고, 프로덕션 키가 나와 실제 응답을 본 뒤에 정한다.

## 백엔드

`server/`의 Cloudflare Worker다(TypeScript, Hono). 저장은 D1이다. 무료 플랜은 카드 없이 쓰고 한도를 넘으면
과금이 아니라 오류로 막힌다. KV는 하루 쓰기가 1,000번뿐이고 R2는 카드 등록이 필요해서 쓰지 않는다. 계정이
생기기 전까지는 `wrangler dev`로 로컬에서만 돌린다. 실행과 배포 순서는 `server/README.md`에 있다.

기대는 무료 한도는 요청 하루 10만, 요청당 CPU 10ms, 서브요청 50, D1 쓰기 하루 10만 행과 DB 500MB, 정적 에셋
파일 2만 개(파일당 25MiB)다. 한 요청이 하는 일을 작게 둔다.

- 앱은 서버가 준 세션 토큰만 든다. Riot 키와 RSO `client_secret`은 서버 비밀값이다. 세션 토큰과 로그인 코드는
  SHA-256 해시만 저장한다. Riot 토큰은 계정을 한 번 읽고 버린다.
- RSO는 앱과 서버 사이에 PKCE를 한 겹 더 둔다. 앱이 verifier를 만들어 challenge만 넘기고, 콜백은
  `ovalit://auth?code=`로 일회용 코드를 준다. 가로챈 코드는 verifier 없이 세션으로 못 바꾼다.
- `/auth/dev`는 RSO 없이 친구 흐름을 돌려 보는 문이다. `DEV_LOGIN=true`이고 로컬 주소로 들어왔을 때만 열린다.
  배포 환경에는 넣지 않는다.
- 전적에 닿는 경로는 모두 세션이 있어야 한다. VAL-CONTENT와 점검 안내도 레이트 리밋을 지키려고 세션 뒤에 둔다.
- 남의 경기는 서로 수락한 친구가 전적을 공개했을 때만 내려준다. 내가 안 뛴 친구 경기는 친구와 나 말고 모두
  가린다. PUUID는 `anon-N`으로 바꾸고 이름·카드·칭호를 지우며, 파티 ID도 경기 안에서만 통하는 값으로 바꾼다.
- 친구 요청은 같이 뛴 경기를 확인한 뒤에만 받는다. 상대가 앱을 쓰는지는 같이 뛴 사람에게만 알려준다.
- 끝난 경기의 참가자는 D1 `match_players`에 한 번 적어 두고 친구 요청과 앱 사용자 확인에 쓴다. Cache API는
  `*.workers.dev`에서 담기지 않아서 isolate 메모리를 먼저 본다. 둘 다 있으면 쓰는 정도이고 앱이 경기를 저장한다.
- 플레이어 카드는 정적 에셋 `/cards/{소문자 uuid}_{small|wide}.png`다. 저장소에 넣지 않고
  `server/scripts/prepare-assets.mjs`가 카탈로그에서 채운다.
- 티어 이름과 요원 역할 표는 `server/src/data/`에 두고 내려준다. valorant-api.com 자료로 스크립트가 만든다.
- 라이벌은 서버에 두지 않는다. 기기에만 있다.

## 비용

무료가 아니면 쓰지 않는다. 유료 구간이 있는 선택지는 제안하기 전에 무료 한도부터 적는다.

- 백엔드는 무료 한도 안에서 돌아가야 한다. 카드 등록 없이 쓸 수 있고, 한도를 넘으면
  과금이 아니라 차단되는 곳을 고른다.
- 앱과 Worker에 들어가는 라이브러리와 폰트는 상업 이용이 무료인 라이선스만 쓴다. Apache-2.0, MIT, SIL OFL.
  배포물에 들어가지 않는 빌드·테스트 도구는 무료면 된다(wrangler가 끌어오는 sharp의 LGPL 등).
- 유료 SDK, 유료 분석 도구, 유료 CI는 넣지 않는다.
- Riot API는 개발용 키와 프로덕션 키 모두 무료다. 대신 레이트 리밋이 유일한 제약이라
  끝난 경기를 다시 요청하지 않는 규칙이 여기서도 중요하다.

피할 수 없는 비용은 Google Play 개발자 등록 1회 25달러 하나다. 출시 직전에만 든다.
그 전까지 드는 돈은 없어야 한다.

## 데이터

| API | 경로 | 용도 |
| --- | --- | --- |
| VAL-MATCH-V1 | `/val/match/v1/matchlists/by-puuid/{puuid}` | 내 경기 ID 목록 |
| | `/val/match/v1/matches/{matchId}` | 경기 상세 |
| VAL-CONTENT-V1 | `/val/content/v1/contents?locale=ko-KR` | UUID → 한글 이름 |
| VAL-STATUS-V1 | `/val/status/v1/platform-data` | 서버 점검 안내 |

한국 샤드 호스트는 `kr.api.riotgames.com`. PUUID 조회는 `asia.api.riotgames.com`의 account-v1.

경기 상세에 라운드별 이코노미와 킬 타이밍까지 내려온다. 지표는 대부분 여기서 계산한다.

| 지표 | 계산 |
| --- | --- |
| 피해량 (ADR) | `damage[].damage` 총합 ÷ `roundsPlayed` |
| 헤드샷 비율 | `headshots ÷ (headshots + bodyshots + legshots)` |
| 전투점수 (ACS) | `players[].stats.score ÷ roundsPlayed` |
| 관여율 (KAST) | 라운드별 킬·어시·생존·트레이드 중 하나 이상 |
| 퍼블 승률 | 내가 퍼블을 딴 라운드 중 이긴 라운드 ÷ 내 퍼블 수 |
| 퍼블 관여율 | (퍼블 + 퍼데) ÷ `roundsPlayed` |
| 첫 교전 승률 | 퍼블 ÷ (퍼블 + 퍼데) |
| 무기별 성적 | `kills[].finishingDamage.damageItem` |
| 교전 거리 | `playerLocations` ↔ `victimLocation` 좌표차 |
| 클러치 | 킬 시각 순으로 우리 팀이 나만 남았을 때 상대가 남아 있었는지 판정 |
| 에이스 | 한 라운드에 상대 팀 모두를 내가 잡았는지 |

무기별 킬은 `economy.weapon`이 아니라 `finishingDamage.damageItem`을 쓴다. `economy.weapon`은
라운드 시작 로드아웃이라 주워 쓴 총이 안 잡힌다. 대신 죽을 때 든 무기와 피해를 준 무기는 응답에 없어서, 무기별
데스·어시스트·라운드당 피해량은 그 무기를 들고 시작한 라운드(`economy.weapon`)로 센다.

클러치는 나만 남은 순간 살아 있던 상대 수로 "1대3"이라 부르고, 그 라운드를 이기면 성공이다. 스파이크를 설치하고
죽었는데 터져서 이긴 것도 성공으로 친다. 내가 먼저 죽었거나 처음부터 우리 팀이 나 혼자면 클러치가 아니다. 튕겨서
그 라운드를 안 뛴 우리 팀은 킬 기록에 안 나와 살아 있는 것처럼 보이므로 그런 라운드는 클러치로 잡히지 않는다.
라운드제가 아닌 모드에는 에이스와 클러치가 없다. 시작 기준선이다.

계산에서 지키는 것들. 전부 `core/model`에 테스트가 있다.

- 총량을 더한 뒤 나눈다. 경기마다 비율을 먼저 구해 평균 내면 13-2 경기와 13-11 경기가
  같은 무게로 섞인다. 평균의 평균은 만들지 않는다.
- 비율의 분모가 0이면 비워 둔다. 0으로 채우면 "헤드샷 0%"처럼 틀린 숫자가 뜬다.
  데스가 없는 경기의 K/D도 비워 둔다.
- 라운드 수는 내가 뛴 라운드만 센다. 튕겼다 들어온 경기에서 전체 라운드로 나누면 ACS와
  ADR이 실제보다 낮게 나온다.
- 헤드샷 비율은 맞힌 탄 기준이다. 킬 중 헤드샷 킬의 비율이 아니다.
- 스킬로 자기를 죽인 것과 우리 팀을 죽인 것은 킬로도 어시로도 세지 않는다. 응답에는 둘 다
  킬로 들어온다. 자기를 죽인 건 데스로는 센다.
- 퍼블과 퍼데는 라운드에서 처음 나온 적 처치로 가린다. 목록 순서가 아니라
  `timeSinceRoundStartMillis`로 본다. 자기 스킬 사망과 팀킬은 건너뛴다. 그래서 내가 스킬로
  먼저 죽은 건 데스지만 퍼데는 아니다.
- 퍼블 승률은 내 퍼블 기준이다. 우리 팀 누군가가 퍼블을 따서 이긴 라운드는 넣지 않는다.
  그 숫자는 내 기여와 상관없이 팀 상황을 따라 움직인다.
- 트레이드는 내가 죽은 뒤 5초 안에 **우리 팀이** 내 킬러를 잡은 경우다. 내 킬러가 자기
  스킬로 같이 죽은 건 아니다. 5초는 시작 기준선이고 실데이터를 보고 조정한다. 바꾸면 KAST가
  통째로 움직인다.

개발용 키로 VAL-STATUS, VAL-CONTENT, account-v1은 열리고 VAL-MATCH는 403이다(2026-09-25 확인).
경기 데이터와 RSO는 프로덕션 키가 있어야 해서, 지표 계산에 쓰는 실데이터는 그 뒤에 붙인다.
개발용 키는 24시간마다 새로 받는다. 키는 `local.properties`의 `riot.api.key`에만 두고 앱이나 저장소에
넣지 않는다.

VAL-CONTENT에서 확인한 것:

- 맵은 UUID `id`와 경로 `assetPath`(`/Game/Maps/Ascent/Ascent`)를 둘 다 준다. 경기 응답의 맵
  ID가 어느 쪽이든 이름으로 바꿀 수 있다.
- 요원(`characters`)에는 `id`, `name`, `assetName`뿐이고 역할이 없다.
- 무기(`equips`)에는 계열이 없고 스킬 장비까지 섞여 있다.
- 액트는 `acts`에 에피소드와 액트가 `parentId`로 이어져 있고, 지금 액트는 `isActive`다.

콘텐츠 카탈로그(13.06)에서 확인한 것:

- 무기(`weapons[].category`)에 계열이 있다. `Rifle`, `Sidearm`, `SMG`, `Sniper`, `Shotgun`, `Heavy`,
  `Melee`와 스킬 장비용 `Hidden`이다.
- 역할(`characterRoles`)은 이름과 아이콘만 있고, 요원에서 역할로 가는 연결이 없다.
- 티어는 이름도 엠블럼도 없다. VAL-CONTENT와 개발자 문서에도 없고, 경기 응답은 번호만 준다.
- 이미지는 요원 얼굴(`{id}.png`)과 전신(`_full`), 맵 목록용 띠(`_listview`)와 스플래시(`_splash`),
  무기, 역할 아이콘, 플레이어 카드(`_small`, `_wide`, `_large`)가 있다.

역할표와 티어 이름·엠블럼은 커뮤니티 API(valorant-api.com)에서 받아 우리 서버에 두고 내려준다.
tracker.gg, op.gg, dak.gg, Blitz도 게임 파일에서 뽑은 엠블럼을 자기 서버에 올려 쓴다. 앱 코드에는
번호와 이름을 잇는 표를 두지 않는다.

아직 확인하지 못한 것:

- 라운드마다 내가 공격인지 수비인지. API 문서의 `roundResults[]`에 이긴 팀(`winningTeam`)과 그 팀의
  진영(`winningTeamRole`)이 있어서, 내 `teamId`와 맞춰 보면 가릴 수 있다. 값이 실제로 어떻게 오는지는
  응답으로 확인한다.
- 경기 응답의 맵 ID가 UUID인지 경로 문자열인지

## 지표 규칙

고정 4개와 동적 3~5개로 나눈다.

고정은 전투점수, K/D, 피해량, 헤드샷이고 항상 같은 자리, 같은 순서에 둔다. 일곱 칸을 전부
동적으로 돌리면 자기 성적이 어떻게 흘러가는지 따라갈 수 없다.

동적 칸에는 관심사 지표와 그 주에 움직인 지표를 둔다. 지금 후보는 관여율, 라운드 생존율, 퍼블 승률,
퍼블 관여율, 첫 교전 승률, 라운드당 어시스트, 이코 승률, 포스바이 승률, 풀바이 승률이다.
사이트별·무기별 성적은 응답에서 그 데이터를 옮겨 담은 뒤에 넣는다. 고정 4개는 후보가 아니다.
넣으면 한 화면에 같은 지표가 두 번 뜬다.

이코·포스바이·풀바이는 우리 팀 한 사람당 평균 장비 가치(`economy.loadoutValue`)로 가른다. 2,000
미만이 이코, 3,900(라이플과 중형 방어구) 이상이 풀바이, 그 사이가 포스바이다. 전반과 후반 첫 라운드는
피스톨 라운드라 셋 어디에도 넣지 않는다. 경쟁과 일반에서만 가른다. 공식 기준이 없어 시작 기준선이다.

후보에 오르려면 두 조건을 다 넘겨야 한다.

1. 최소 표본을 넘길 것. 이번 기간, 비교 기준, 변동폭을 재는 각 주가 모두 넘겨야 한다.
2. 변화량이 본인 평소 주간 변동폭보다 클 것. `|이번 기간 − 지난 4주 평균|`이 지난 8주 주간
   값의 표본 표준편차의 1.5배를 넘어야 한다. 표본을 넘긴 주만 쓰고, 그런 주가 4주 미만이면
   판단하지 않는다.

| 지표 | 최소 표본 |
| --- | --- |
| 관여율 · 라운드 생존율 · 퍼블 관여율 · 라운드당 어시스트 | 40라운드 |
| 퍼블 승률 | 내 퍼블 10번 |
| 첫 교전 승률 | 첫 교전 15번 (퍼블 + 퍼데) |
| 이코 승률 · 포스바이 승률 | 그 라운드 15번 |
| 풀바이 승률 | 풀바이 라운드 40번 |

1.5배와 최소 표본은 시작 기준선이고 실데이터를 보고 조정한다.

관심사 지표를 맨 앞에 늘 둔다. 사용자가 보겠다고 고른 지표라 움직이지 않았어도, 역할이 크게 띄우지 않는
지표여도 넣고 관심사에 적힌 순서를 지킨다. 그 뒤에 움직인 지표를 역할의 우선 지표, 많이 움직인 순(변화량 ÷
변동폭)으로 붙여 5개까지 둔다. 3개가 안 되면 기본 3개(관여율, 라운드 생존율, 퍼블 승률) 순서로 채운다. 홈은
한 줄에 세 칸씩 놓고 넘치면 다음 줄로 넘긴다. 관심사는 S0-4와 설정에서 고른다. 에임 올리기는 첫 교전 승률,
라운드 운영 다듬기는 포스바이·이코·풀바이 승률, 기복 줄이기는 관여율과 생존율이고, 특별히 없어요는 변화량만 본다.
"이번 주는 큰 변화 없음"은 하나도 움직이지 않았을 때만 띄운다. 기록이 모자라 판단을 보류한
칸(`Movement.UNKNOWN`)을 "큰 변화 없음"으로 띄우지 않는다. 변화가 없는 게 아니라 모르는 것이다.

카드에는 근거를 함께 적는다. 총량보다 판당 평균이 읽기 쉽다.
예: `헤드샷 24% · 지난 4주 평균 19% · 11경기, 판당 16킬`

동적 칸마다 비교 기준과 표본을 적는다(`4주 평균 31%`, `라운드 496`, `퍼블 14번`). 표본은
그 비율의 분모다.

주는 기기 시간대 기준 월요일 0시에 바뀐다. 이번 주는 아직 끝나지 않은 주다. 최근 7일처럼
굴러가는 창은 쓰지 않는다. 새 경기가 없어도 오래된 경기가 창 밖으로 빠지면서 숫자가 매일
바뀐다.

이번 주에 뛴 경기가 없으면 지난주에서 끝나는 기간을 본다(`지난주`, `지난 2주`). 월요일
아침에 `최근 2주`로 넓히면 전부 지난주 경기인데 이번 주가 섞인 것처럼 읽힌다. 이번 주에 한
판이라도 뛰었으면 이번 주부터 본다.

그 기간이 5경기를 못 채우면 한 주씩 넓혀 최대 4주까지 보고(`최근 2주`) 카드에 기간을
표시한다. 4주로도 못 채우면 리포트 대신 뛴 경기 수를 보여준다. 4주 동안 한 판도 없으면
남은 경기 수 대신 쉬었다는 말부터 한다.

카드의 "지난 4주 평균"은 집계 기간 바로 앞 4주다. 기간을 넓히면 그만큼 뒤로 밀린다. 이
4주도 5경기를 못 채우면 비교하지 않는다. 집계 최대 4주와 비교 4주를 합치면 첫 수집 범위
8주와 맞는다.

액트 경계를 넘는 평균은 만들지 않는다. 액트마다 랭크가 초기화되고 매칭 난이도가 달라진다.
리포트는 가장 최근 경기의 액트만 센다. 비교 기준도 이번 액트 첫 경기가 있는 주부터 세고
그만큼 기간이 줄어든다(`지난 2주 평균`). 그래서 새 액트 초반에는 5경기를 채울 때까지 리포트가
없다. 추이 그래프에서는 경계에 전환 마커를 찍고 평균을 새로 시작한다.

큐 기본값은 경쟁 + 일반이다. 두 모드는 13라운드 선취에 이코노미 규칙이 같아 합산해도
통계가 깨지지 않는다. 스파이크 돌격(무기를 랜덤으로 주고 4라운드 선취)과 신속 플레이(크레드
규칙이 다르다)는 기본에서 뺀다. 기타 모드는 별도 탭으로 빼고 KDA와 헤드샷 비율만 쓴다.
홈 위쪽 칩으로 경쟁 + 일반 · 경쟁 · 일반 · 기타를 고른다(`QueueFilter`). 기타에는 동적 칸이 없다.

## 역할군

역할은 `characterId`로 알아내고 유저에게 묻지 않는다. 같은 수치라도 역할에 따라 해석이
반대로 뒤집힌다. 전략가의 퍼블 관여율 12%는 정상이고 타격대면 문제다.

| 역할 | 우선 지표 | 크게 띄우지 않는 것 |
| --- | --- | --- |
| 타격대 | 퍼블 관여율, 첫 교전 승률, K/D | 어시스트 |
| 척후대 | 어시스트, 관여율, 스킬 활용 | K/D |
| 전략가 | 관여율, 생존율, 공수별 성적 | 퍼블, K/D |
| 감시자 | 생존율, 클러치, 사이트 수비 | 퍼블 |

역할에 따라 달라지는 건 동적 칸과 개선 포인트 문장이다. 고정 4개는 그대로 둔다.

그 주의 역할은 기간 중 라운드를 가장 많이 뛴 역할이다. 우선 지표는 움직였을 때 관심사 지표 다음에 두고,
크게 띄우지 않는 것은 동적 칸 후보와 빈칸 채우기에서 모두 뺀다. 관심사로 고른 지표만 예외다. 그래서 전략가와
감시자는 기본 3개에서도 퍼블 승률이 빠지고 라운드당 어시스트로 채우지만, 에임 올리기를 고르면 첫 교전 승률은
뜬다. K/D는 고정 칸이라 동적 칸에 오르지 않는다. 스킬 활용, 공수별 성적, 클러치, 사이트 수비는 계산할 데이터가
생기면 후보에 넣는다.

## 용어

앱 이름 "오발있"은 "오늘 발로란트 할 사람 있어?"를 줄인 말이다. 로고가 초성 `ㅇㅂㅇ`인 것도
여기서 나왔다. 카피에서 "오발있?"을 쓰면 브랜드와 말 걸기가 한 번에 된다. 인트로에서 로고
바로 밑에 두는 이유도 마크를 소리 내어 읽어주기 위해서다.

풀어 쓸 때는 이 문장 그대로 쓴다. 화면에도 같은 문장이 들어가 있으니 둘이 갈라지면
어느 쪽이 맞는지 알 수 없다.

유저가 실제로 쓰는 말만 쓴다.

| 쓴다 | 쓰지 않는다 |
| --- | --- |
| 요원 | 에이전트 |
| 경기, 판 | 매치 |
| 타격대 · 전략가 · 척후대 · 감시자 | 듀얼리스트 · 컨트롤러 · 이니시에이터 · 센티널 |
| 이코 · 포스바이 · 풀바이 · 세이브 | 에코 · 강제 |
| 퍼블 · 퍼데 | 첫 킬 · 첫 데스 |
| 스파이크 돌격 / 신속 플레이 | 신속 |

화면 라벨은 한글로 쓰고 정식 명칭은 설명 시트에서 알려준다.
전투점수(ACS), 피해량(ADR), 관여율(KAST), 헤드샷, K/D.

위 표는 화면과 문서에 쓰는 말이다. 코드 식별자는 영어로 둔다(`Queue.SPIKE_RUSH`).
한글 식별자는 스택 트레이스와 도구에서 다루기 어렵다. 대신 화면에 뜨는 이름은 반드시
문자열 리소스를 거친다. 식별자를 그대로 화면에 띄우지 않는다.

UI 카피에서 "복기"는 쓰지 않는다. "돌아보다" 쪽으로.

말투는 해요체다. "짚어드려요", "다시 불러올까요". 합니다체는 딱딱하고 반말은 전적 앱에
안 맞는다. 법적 고지만 예외로 합니다체를 쓴다.

화면에 뜨는 문구는 Kotlin 상수로 두지 않는다. 모듈별 `composeResources/values/strings.xml`에
넣고 `stringResource`로 읽는다. 로케일을 늘릴 때 `values-en`을 옆에 두면 끝나야 한다.
줄바꿈을 `\n`으로 박지 않는다. 기기 폭과 언어가 바뀌면 깨진다.

## 디자인

참고하는 앱은 Strava(기록 추적과 친구 비교), 토스(타이포 위계), Spotify·Linear(다크 UI에서
테두리 대신 배경 단계), 핀터레스트다.

피하는 것: 좌측 컬러 바가 붙은 카드, 모든 요소를 테두리 있는 둥근 카드로 감싸기,
보라·남보라 액센트, 알약 뱃지 남발, 그라데이션, 글래스모피즘, 이모지 아이콘,
Inter / Roboto / 나눔고딕. 그라데이션은 이미지 위에 글자나 아이콘을 얹을 때 까는 흐림막에만 쓴다.

MaterialTheme은 `OvalitTheme` 안에 우리 토큰을 옮겨 담아 같이 깐다. Material 컴포넌트를 써도 색, 글꼴,
누름 효과가 맞게 나온다. 화면 코드는 `MaterialTheme.colorScheme`이 아니라 `OvalitTheme.colors`를 쓴다.

누름 효과는 `OvalitPressIndication` 하나다. 누르는 즉시 옅은 면을 깔고 누른 것을 살짝 줄였다가(최대 4%, 긴 줄은
양옆 4dp) 떼면 돌아온다. 물결은 쓰지 않는다. 누른 뒤에 천천히 퍼져서 늦게 반응하는 것처럼 보인다. 면은 보이는
모양에만 깐다. 기본 모양은 모서리 12dp 둥근 사각형(`OvalitPressShape`)이고 네모 그대로나 원은 쓰지 않는다.
눌리는 영역이 44dp라도 34dp 버튼이면 버튼 모양에 깔고, 면을 칠한 버튼은 면까지 같이
줄도록 `clip`과 `background`보다 앞에 단다. 무엇을 보여 줄지 고르는 곳은 지금 고른 것을 적은
`OvalitPickerButton`과 `OvalitSheetOption`을 담은 바텀시트로 만든다(홈 친구 비교, S7 요원 표, 설정). 접었다 펴는 곳은 `OvalitExpandable`로 높이가 부드럽게 바뀌게 하고 화살표를 돌린다.
지표 숫자가 바뀌면 `OvalitRollingText`로 바뀐 자리의 숫자만 굴린다. 단위(%)와 변화량은 움직이지 않는다. 배치는
글자 하나로 잡고 구르는 모습은 그리는 단계에서만 덧그린다. 화면은 어떤 폭과 글꼴 크기에서도 깨지면 안 된다.
나란한 칸(홈 고정·동적 칸, S5 기간 칸, 내 프로필 통계, 나와 비교)은 칸마다 따로 줄이지 않고 가장 긴 글자에 맞춘
한 크기를 모든 칸에 쓴다(`rememberFittingStyle`). 칸 안에서 줄을 꺾을 때도 한 칸이 넘치면 모든 칸을 같이 꺾는다.
한 칸만 작아지거나 꺾이면 그 칸만 높이가 달라 줄이 어긋난다. 아래 고정 버튼 위로 본문이 이어지면 선을 긋는다. 햅틱(`rememberOvalitHaptics`)은 친구 수락·거절, 요청,
라이벌 지정, 설정 토글처럼 확정하는 동작에만 쓴다. 스크롤, 탭 이동, 칩에는 쓰지 않는다.

강조는 색이 아니라 밝기 단계와 굵기로 한다. 액센트를 데이터에 쓰면 위계가 무너진다. 예외는 목업을
따른 곳들이다. 지표 설명 시트의 추이 막대는 리포트 기간에 든 주만 `--accent`로 칠한다. 요원 화면의
역할 막대와 무기 화면의 계열 막대는 가장 많은 한 줄만 `--accent`다. S0-4에서 고른 관심사 칸은 테두리와
체크가 `--accent`다. 스코어보드의 "친구" 표시는 `--accent-ink`다.

요원별 승률, 내 프로필 티어 칸의 경쟁전 승률, 홈 기간 승률은 보이는 %가 50을 넘으면 `--pos`, 밑돌면 `--neg`로
칠한다. 경기 결과(경기 줄의 스코어, S3의 내 팀 점수와 "우리 팀", 홈의 승패 칸)는 이기면 `--pos`, 지면 `--neg`다. S3 라운드
막대는 목업대로 이긴 라운드만 `--pos`이고 진 라운드는 `--bar`다. 변화량이 아닌데 색을 쓰는 곳은 이것뿐이다.

변화량은 오르면 `--pos`, 내리면 `--neg`로 칠한다. 고정 칸은 늘 칠하고, 동적 칸은 움직였다고
판단한 칸만 칠한다. 평소 범위 안의 변화에 색을 칠하면 흔들림이 경고처럼 읽힌다. 퍼블 관여율은
역할에 따라 오르는 게 좋기도 나쁘기도 해서 움직여도 색을 칠하지 않는다.

변화량은 원래 값이 아니라 화면에 보이는 자릿수로 반올림한 값끼리 뺀다. 74%와 69%를 나란히
띄워 놓고 변화량에 +6을 쓰면 틀려 보인다.

서체는 Pretendard Variable 한 벌이다. SIL OFL이라 상업 번들이 자유롭고, 가변 폰트라 파일
하나로 9굵기를 다 쓴다(`FontVariation`은 API 26+라 minSdk와 맞는다).

한글 획이 얇아서 굵기 이름보다 한 단계 굵게 그린다. SemiBold는 `wght` 700, Bold는 800이고 Normal과 Medium은
400, 500 그대로다. 600으로는 제목과 숫자가 본문과 덜 갈렸다. Bold도 꼭 등록한다. 빠지면 "굵게"를 달라고 한
자리(스코어, 앞선 값)가 가장 가까운 SemiBold로 그려져 구분이 안 된다.

숫자도 같은 서체로 간다. 지표 숫자는 `FontFeatureSetting`의 `tnum`으로 폭을 고정한다.
자릿수가 바뀌어도 칸이 흔들리지 않아야 추이가 읽힌다.

라틴 글리프가 Inter 기반이라 위의 "피하는 것"과 겹쳐 보인다. 그 항목은 아무 데나 Inter를
올린 기본값 UI를 가리킨 것이고, 한글 본문용 Pretendard는 다른 얘기다.

줄바꿈은 둘로 나눈다. 제목과 숫자는 줄 길이를 고르게 나눠 마지막에 한 단어만 남는 걸 막는다. 본문과 설명은
줄을 끝까지 채우고 어절 가운데서만 끊지 않는다. 두세 줄짜리 설명까지 고르게 나누면 첫 줄이 짧아져 "볼 / 수
없어요"처럼 말이 갈린다.

### 색 토큰

원시 hex를 화면에 직접 쓰지 않는다.

| 토큰 | 다크 | 라이트 |
| --- | --- | --- |
| `--bg` | `#100E0C` | `#FDFCFA` |
| `--raised` | `#1A1714` | `#F7F4F0` |
| `--line-weak` | `#1C1917` | `#EFEAE4` |
| `--line` | `#23201C` | `#E5DFD7` |
| `--fill` | `#2A2621` | `#F2EDE7` |
| `--bar` | `#322D27` | `#EAE5DE` |
| `--t1` | `#F5F2ED` | `#191510` |
| `--t2` | `#9A9289` | `#6E675E` |
| `--t3` | `#6D665E` | `#928B82` |
| `--t4` / `--t5` | `#57514A` / `#443F39` | `#ACA49A` / `#C8C1B8` |
| `--pos` | `#3FCF8E` | `#0E8A52` |
| `--neg` | `#E5484D` | `#C2262C` |
| `--accent` | `#E0B252` | `#E0B252` |
| `--accent-ink` | `#E0B252` | `#8A6410` |
| `--on-accent` | `#100E0C` | `#100E0C` |

라이트는 다크를 그대로 뒤집은 게 아니다. `--pos`와 `--neg`를 흰 바탕에 그대로 올리면 대비가
2:1 근처까지 떨어지므로 둘 다 어둡게 내린다. `--accent`는 버튼 면일 때만 금색 그대로 쓰고
텍스트로 쓸 때는 `--accent-ink`로 내린다.

배경을 회색이 아니라 따뜻한 쪽에 둔 건 의도다. 게임 에셋의 붉은 기와 나란히 놓으면 색이 맞는다.

### 로고

초성 `ㅇㅂㅇ`. 획 11, ㅂ 아래 모서리 반경 8, ㅇ 기준으로 ㅂ을 8.5 내린다.

```
viewBox 0 0 160 74, stroke-width 11, linecap/linejoin round
circle cx=27  cy=32.5 r=17.5
path   M61.5 23v28a8 8 0 0 0 8 8h21a8 8 0 0 0 8-8V23
path   M61.5 37.5h37
circle cx=133 cy=32.5 r=17.5
```

적응형 아이콘은 `--accent` 바탕에 `--on-accent` 마크다. 아이콘은 면을 칠하는 자리라 버튼과
같은 쌍을 쓴다. 테마를 따르지 않는다. 런처는 앱의 다크 설정을 모른다.

화면 안의 로고는 다르다. 면이 아니라 글자에 가까우므로 `--t1`을 쓴다.

적응형 아이콘은 108dp 캔버스에 마크 폭 50dp. 런처가 실제로 보여주는 건 캔버스 가운데
지름 72dp뿐이고, 50dp 마크는 그 원의 70%를 차지한다. 다른 앱 아이콘의 글리프도 이 정도다.

75dp까지는 잘리지 않지만 쓰면 안 된다. 원에 꽉 차서 답답하고, 가로로 긴 마크라 더 심하게
보인다. 잘리는 한계와 보기 좋은 크기는 다른 값이다.

스플래시도 같은 쌍을 쓴다. 런처 아이콘에서 이어지는 화면이라 색이 끊기면 안 된다.
둘 다 시스템 테마를 따르지 않는다. 창 배경만 테마를 따른다.

캔버스 규격은 다르다. 런처는 108dp에 마크 50dp, 스플래시는 288dp에 마크 132dp다.
그래서 드로어블 파일은 따로 둔다. 알림 작은 아이콘(`ic_notification.xml`)은 24dp에 흰색 한 가지다. 같은
마크를 네 군데서 그리는 셈이니 로고를 고치면 `OvalitLogo`, `ic_launcher_foreground.xml`,
`ic_splash_mark.xml`, `ic_notification.xml`을 다 고친다.

스플래시에 애니메이션은 넣지 않는다. 시스템이 애니메이션을 기다려주지 않아서, 보이게 하려면
`setKeepOnScreenCondition`으로 앱 시작을 일부러 늦춰야 한다. 그 값어치가 없다고 판단했다.
자세한 경위는 `docs/DECISIONS.md`에 있다.

## 화면

```
S0-1  인트로
S0-2  연동 동의  →  S0-3  Riot 로그인 (Custom Tabs)
S0-4  불러오는 중
  ↓
S1    홈 (주간 리포트)
  ├─ S1-a  지표 설명 시트
  ├─ S2    경기 목록  →  S3  경기 상세  →  S5  플레이어 프로필
  ├─ 내 프로필 (오른쪽 위 아바타)  →  S6 무기, S7 요원
  └─ S4    설정
```

S0-3 Riot 로그인은 RSO가 붙기 전까지 건너뛰고, S0-2의 계속하기가 가짜 계정으로 연동한다.
`core/data`의 `FakeMatchRepository`가 가짜 경기를 만들고 S0-4에서 한 판씩 받는 것처럼 채운다.

S1은 목업 중 지금 계산할 수 있는 부분까지만 있다. 누를 곳이 없는 화살표와 탭은 미리 그리지 않는다.
맨 위는 글자가 아니라 ㅇㅂㅇ 로고고, 오른쪽 위는 티어 엠블럼, 티어 이름, 플레이어 카드 아바타다. 셋을
한 번에 눌러 내 프로필로 간다. 기간 줄의 역할은 "타격대 78%"처럼 그 역할로 뛴 라운드 비중을 붙여
적는다(`mainRoleText`). 역할만 적으면 그 기간에 그 역할만 한 것처럼 읽힌다. 비중의 분모는 역할을 아는 라운드다.
역할 이름만 `--t2` SemiBold로 한 단계 올린다. 그 주
동적 칸과 개선 포인트가 이 역할로 골라지니 한눈에 들어와야 한다. 기간 줄의 강조는 이 한 곳뿐이다. 고정 칸 밑
줄에는 판당 K/D/A 옆에 "KDA 2.13"을 붙이고 숫자만 한 단계 밝고 굵게 둔다.

기간 줄 밑에는 기간 경기의 승패를 오래된 경기부터 한 칸씩 칠하고(이기면 `--pos`, 지면 `--neg`, 비기면 `--bar`)
"4승 2패 · 67%"를 적는다. 비긴 경기는 승률 분모에서 뺀다. 칸은 가장 최근 20경기까지만 두고, 넘치면 칸 앞에 "최근
20경기"를 적는다. 한 주에 수십 판을 뛰면 칸이 바코드처럼 줄었다. 승패 글자는 기간 전체를 센다. 개선 포인트 밑에는 "이번 주 요원"과 "이번 주 무기"를 내
프로필과 같은 세 칸으로 두고, 누르면 S7과 S6으로 간다. S6과 S7은 이번 액트 전체를 봐서 숫자가 다르니 제목에 기간을
붙인다. 요원은 기간이 짧아 5판을 못 넘기는 일이 많아 판 수 대신 "2승 1패"를 적고, KDA는 판 수와 상관없이 그 기간
합계로 적는다. 승률은 5판을 넘길 때만 붙인다. 기타 모드에는 요원·무기 칸이 없다.

S1-a 지표 설명 시트는 고정 지표 칸을 누르면 뜬다. 한글 이름 옆에 정식 약어, 한 줄 설명, 이번 기간
값과 변화량, 주별 막대, 평소 범위, 실제 합계로 쓴 계산식 순서다. 목업의 "어느 정도면 괜찮나요?"는
프로 평균 같은 남의 기준과 비교해서 "평소에는 어느 정도였나요?"로 바꿨다. 기간 앞 주들 중 이번
액트이고 값이 있는 주의 가장 낮은 값과 높은 값이고, 그런 주가 4주 미만이면 말하지 않는다. 막대는
기간 마지막 주에서 끝나는 8주다(첫 수집 범위). 라운드가 40에 못 미치는 주는 비우고, 한 주에 액트가
둘이면 새 액트 경기만 세며, 액트가 바뀐 곳에 세로선을 긋는다. 막대 높이는 0이 아니라 가장 낮은 값
근처에서 시작한다.

개선 포인트 문장은 달라진 점 아래에 하나만 둔다. 기간 경기를 공격과 수비로 나눠 가장 벌어진 지표를
사실로만 적는다("수비 라운드 생존율이 공격보다 13%p 낮아요." / "공격 48%, 수비 35%예요."). 후보는
생존율, 관여율, 첫 교전 승률, 피해량이다. 고르는 순서는 셋이다.

1. 관심사 지표(에임 올리기는 첫 교전 승률, 기복 줄이기는 관여율과 생존율, 라운드 운영 다듬기는 이코·포스바이·풀바이
   승률) 중 기준을 넘는 것이 있으면 그중 가장 벌어진 것을 고르고 "에임 올리기를 고르셔서 먼저 봤어요"를 붙인다.
   역할이 크게 띄우지 않는 지표여도 본다. 이코·포스바이·풀바이 승률은 라운드 운영을 골랐을 때만 후보다.
2. 없으면 역할의 우선 지표(전략가·감시자 생존율, 척후대 관여율, 타격대 첫 교전 승률)가 기준을 넘을 때 그것을 고르고
   "전략가에게 생존율은 먼저 보는 지표예요"를 붙인다.
3. 아니면 나머지 중 기준 대비 가장 벌어진 것이고, 역할에서 크게 띄우지 않는 지표는 뺀다.

기준은 보이는 %끼리 10%p, 피해량은 기간 평균의 15%이고, 공격과 수비가 각각 동적 칸과 같은 최소 표본(40라운드,
첫 교전 15번, 이코·포스바이 15번, 풀바이 40번)을 넘겨야 한다. 시작 기준선이다. 넘는 게 없으면 문장을 두지 않는다.
기타 모드에는 없다. 목업의 "전략가는 수비에서 오래 버틸수록 라운드가 풀립니다" 같은 일반론은 쓰지 않는다.

하단 탭바는 목업대로 홈, 경기, 친구, 설정 네 개다. 홈은 늘
스택 맨 아래에 있고 다른 탭은 그 위에 하나만 둔다. 어느 탭에서 뒤로 가도 홈이 나오고, 홈에서
뒤로 가면 앱을 나간다. 네 탭의 맨 위 줄은 `OvalitTabHeader`로, 뒤로 가기가 있는 화면은
`OvalitBackTopBar`로 그린다. 둘은 높이가 같아서 어느 화면으로 가도 제목과 버튼이 같은 자리에 있다. 홈은 제목
자리에 로고를 둔다. 머리 줄을 화면에서 따로 그리지 않는다. 탭바에서 고른 탭은 채운 아이콘으로 바꾼다.
홈과 경기 탭은 당겨서 새로 끝난 경기를 받는다(`MatchRepository.refresh`). 받는 중에 또 당기면 무시한다.

친구는 서로 수락해야 맺어진다. 요청은 같이 뛴 경기의 스코어보드나 초대 링크로만 보낸다. Riot
정책상 데이터 공유에 동의(연동)하지 않은 사람의 기록은 다른 사람에게 보여줄 수 없어서, 친구 목록은
서버가 들고 있어야 한다. 기기에만 저장하는 한쪽 팔로우는 그래서 안 된다. 친구 탭은 목업에 없는
화면이라 설정과 같은 짜임으로 그렸다. 초대 링크, 받은 요청(수락·거절), 친구 목록 순서다.

홈의 라이벌 칸은 S5에서 라이벌로 고른 친구가 있을 때만 둔다. K/D, 피해량, 헤드샷을 내 리포트와
같은 기간으로 나란히 놓고, 보이는 자릿수로 앞선 개수를 센다("3개 중 2개 앞섬"). 친구 비교는 나와
전적을 공개한 친구를 고른 지표 순으로 세운다. 그 기간에 경기가 없는 친구는 빠진다. 기타 모드에는
둘 다 없다.

라이벌이 없으면 그 자리를 비우지 않고 유도 칸을 하나 둔다. 리포트, 친구, 라이벌 순서로 본다.

- 리포트를 만들 기록이 모자라면 경기 수 안내가 먼저라 아무것도 권하지 않는다.
- 친구가 한 명도 없으면 초대를 권한다. 누르면 초대 링크 공유 창이 뜬다. 전적을 비공개한 친구도 친구로 센다.
- 친구는 있는데 라이벌이 없으면 라이벌 고르기를 권한다. 누르면 전적을 공개한 친구 목록 시트가 뜨고, 고르면 그
  자리에 라이벌 대결이 뜬다. 고를 수 있는 친구가 없으면 권하지 않는다.

S0-4 첫 수집은 최근 50경기를 받되 8주를 넘는 경기는 잘라낸다. 집계 지표는 수집이 끝난 뒤에
한 번만 띄운다. 헤드샷 24%가 잠시 뒤 19%로 바뀌면 유저는 그 뒤로 숫자를 믿지 않는다. 그 전까지 홈은 숫자 없이
모양만 잡은 스켈레톤(`OvalitSkeleton`)이다. 경기
목록은 값이 바뀌지 않고 늘어나기만 하니 받는 대로 채운다.

S0-4 위쪽에서는 관심사를 고른다(설정에서도 바꾼다). 아래쪽은 받은 경기마다 한 칸씩 이긴 판은
`--pos`, 진 판은 `--neg`로 채우고, 다 받으면 "리포트 보기"가 뜬다. 앱을 닫아도 계속 받고, 끝나면 "분석
완료" 알림을 보낸다. 설정에서 알림을 껐거나 앱을 보고 있으면 보내지 않는다. 알림 권한은 S0-4에 들어올 때
묻는다.

S4 설정에는 전적 공개 토글(기본 켬), 저장된 데이터 삭제, Riot 계정 연동 해제, 맨 아래 비공식
고지가 반드시 들어간다. 표시 묶음에 S0-4에서 고른 관심사를 바꾸는 줄도 둔다. 연동을 해제하면 저장된 경기와 리포트를 모두 지운다. 삭제와 연동 해제는
바텀시트로 한 번 더 묻는다. 설정 값은 DataStore로 기기에 저장하고, 홈은 칩을 고르기 전까지 설정의
기본 큐로 연다. 라이벌 대결 결과 알림은 라이벌이 생길 때 넣는다.

내 프로필은 목업에 없는 화면이다. 홈 오른쪽 위 아바타를 누르면 열린다. S5의 머리(배너, 아바타, Riot ID,
가장 많이 뛴 역할과 비중, "이번 액트 50경기") 아래로 티어 카드, 통계, 맞힌 부위, 요원, 무기, 최근 경기 순서다. 위쪽은
사용자가 고른 op.gg식 목업을 따른다. 머리의 역할 아이콘은 역할 이름 바로 앞에 둔다(`[아이콘] 타격대 78%`).
아이콘은 역할에 딸린 것이다. 역할 비중이 머리에 있으니 요원 칸 제목 줄에는 다시 적지 않는다.

- 최근 경기 말고는 모두 이번 액트의 경쟁 + 일반 경기만 센다.
- 티어 카드는 이번 액트 경쟁전의 지금 티어, 승패, 승률이다. 이 화면에서 면을 까는 곳은 이 카드뿐이다. 티어
  흐름 그래프는 두지 않는다. 티어를 카드에 크게 두니 이름 줄에서는 뺀다. 경쟁전이 없으면 카드가 없고 티어는
  이름 줄에 남는다.
- 통계는 피해량, K/D, 전투점수와 최다 킬, KDA, 플레이 시간, 그 뒤에 에이스 횟수와 클러치("4번 중 2번", 나만 남은
  라운드 중 이긴 라운드)다. KDA 밑에 판당 K/D/A를 작게 붙인다. 숫자만으로는 몇 킬 몇 데스인지 모른다. KDA를 "평점"으로
  부르거나 op.gg의 PS 같은 점수를 만들지 않는다(지켜야 할 선). 나만 남은 라운드가 없으면 클러치는 비운다. 세 칸에
  숫자가 안 들어가면 같은 순서로 두 칸씩 놓는다.
- 맞힌 부위는 맞힌 탄을 머리, 몸, 다리로 나눈 막대다. 머리를 가장 밝게 칠한다. 몸 그림은 쓰지 않는다.
- 요원은 많이 뛴 셋의 얼굴, 판 수, 승률, "KDA 1.83"이고 누르면 S7이다. 승률과 KDA는 5판 이상일 때만 띄운다. 합계까지
  적으면 칸이 무거워서 KDA만 둔다. 무기는 S6 위쪽 세 무기의 이번 액트 킬 수, 헤드샷, 라운드당 피해량이고 누르면
  S6이다. 헤드샷과 피해량은 S6과 같은 표본을 넘겨야 띄운다. 무기도 요원과 같은 세 칸 격자에 놓는다.
- 최근 경기는 큐와 액트를 가리지 않은 세 판이다. 내 경기라 누르면 S3이고 "전체 보기"는 경기 탭이다.

내 프로필과 S5 머리는 상태 표시줄 밑까지 깔리고, 배너는 상태 표시줄 높이만큼 길어진다. 배너 높이를 고정하면 상태
표시줄이 높은 기기에서 아바타가 뒤로 가기를 덮는다. 내려서 배너가 그 자리를 벗어나면 바탕색으로 덮는다.

S6 무기와 S7 요원은 이번 액트의 경쟁 + 일반 경기만 센다. 목업의 "최근 50경기"는 액트 경계를
넘어서 쓰지 않는다. 요원은 5판, 무기는 한 무기만 쓴 라운드 20을 넘겨야 숫자를 띄우고 모자라면
"표본 부족"이다. 주 역할은 라운드 수로 정한다. S7 맨 위 주 역할 이름 앞에는 프로필 머리처럼 역할 아이콘을 글자
크기에 맞춰 둔다. 요원별 표의 오른쪽 두 열은 주 역할이 타격대면 퍼블
관여율과 첫 교전 승률, 아니면 관여율과 생존율로 시작하고, 표 위 버튼으로 K/D · 피해량이나 전투점수 · 헤드샷
묶음으로 바꾼다. 두 열씩 묶어 둬서 무엇을 골라도 표 모양이 그대로다. 고른 건 화면을 떠나면 처음으로 돌아간다. 요원
이름 밑에는 역할, 판 수, "KDA 1.88"을 적는다. KDA는 승률처럼 5판을 넘긴 요원만 적고, 한 줄이라도 이름 밑에 다 안
들어가면 모든 줄에서 KDA를 한 줄 내린다.
무기의 헤드샷은 내 킬이 전부 그 무기로 난
라운드만 센다. 응답에 맞힌 탄이 라운드별로만 있어서다. 계열을 펼치면 무기마다 이름 밑에 KDA와 K/D/A 합계("1.87
(369/267/131)", 들고 시작한 라운드가 20에 못 미치면 합계만), 오른쪽에
K/D, 라운드당 피해량, 헤드샷을 둔다. 위쪽 세 무기 표와 같은 열이다. K/D와 피해량도 그 무기를 들고 시작한 라운드가
20을 넘겨야 띄우고, 둘 다 모자라면 "표본 부족"을 한 번만 적는다. 무엇을 기준으로 셌는지는 표 밑에 적는다.

무기 위쪽은 이번 액트 킬이 많은 세 무기의 표다. 홈 리포트 기간의 K/D, 라운드당 피해량, 헤드샷을 그 앞 4주 평균과
견주고, 열 제목은 맨 위에 한 번만 둔다. 무기 K/D는 그 무기로 낸 킬 ÷ 그 무기를 들고 시작한 라운드의 데스다.

- 변화량은 지표마다 동적 칸과 같은 규칙(평소 주간 변동폭의 1.5배)을 넘을 때만 칠한다. "요즘 잘 맞아요"는 맞히는
  얘기라 헤드샷이 그럴 때만 붙인다.
- 헤드샷(한 무기만 쓴 라운드)과 K/D·피해량(들고 시작한 라운드) 중 하나라도 기간 표본이 20에 못 미치면 그 줄 전체를
  이번 액트 값으로 띄우고 "이번 액트 기준"이라고 적는다. 한 줄 안에서 기간이 섞이면 어느 숫자가 언제 것인지 모른다.
- 이름 밑에는 순서를 정한 이번 액트 킬을 적는다. 기간 킬을 적으면 킬이 적은 무기가 위에 있는 것처럼 보인다.
- 이름 칸에 글자가 한 줄로 안 들어가면 세 줄 모두 숫자를 이름 밑으로 내린다.

카탈로그에 없는 요원과 무기는 "알 수 없는 요원"으로 띄운다.

S5는 나와의 관계에서 시작하는 친구 프로필이다. 머리(라이벌 지정 버튼 포함) 바로 밑에 "같이 한 경기 12경기 8승
4패"를 둔다. 친구 기반 앱만 낼 수 있는 숫자다. 그 아래는 사용자가 정한 순서대로 티어 카드, 통계, 나와 비교(내 리포트
기간), 맞힌 부위, 요원, 무기, 최근 경기다. 티어 카드부터 무기까지는 내 프로필과 같은 `core/ui` 칸을 같은 기준(이번
액트 경쟁 + 일반)으로 쓴다. 친구에게는 요원·무기 상세 화면이 없어서 그 칸은 누르지 않는다. 목업의 "이번 주" 4칸은
뺐다. 전적을 공개하지 않은 친구는 같이 한 경기만 보여주고 라이벌로 고를 수 없다.
맨 아래 "최근 경기"는 세 판을 두고, 더 있으면 "전체 보기"로 친구의 경기 목록을 연다. 친구 경기에는 내가
안 뛴 경기의 다른 사람 기록이 섞여 있어서 줄을 눌러도 열지 않는다. 더보기에서 친구를 끊을 수 있고,
끊으면 라이벌에서도 빠진다. 연동을 해제하면 친구 관계도 모두 지운다.

S2 경기 목록은 날짜로 묶는다(오늘, 어제, "9월 22일 화요일"). 오늘 경기는 "2시간 전", 그 전은 시각만
적는다. 줄은 맵 썸네일에 요원 얼굴을 겹치고, 스코어와 K/D/A · ADR은 응답 숫자를 그대로 쓴다. 큐 칩은
홈과 따로 고르고 처음에는 설정의 기본 큐다. 오른쪽 위 필터로 요원과 맵을 고른다.

S3 경기 상세는 맵 배너, 스코어와 전후반(연장), 라운드 막대, 탭 셋이다. 라운드제가 아닌 모드는 라운드
탭이, 경쟁·일반이 아닌 모드는 이코노미 탭이 없다.

- 스코어보드는 팀마다 전투 점수 순이다. 내 줄은 `--raised`로 깐다. 친구만 화살표가 있고 누르면 S5다.
- 친구가 아닌 사람을 누르면 시트가 뜬다. 앱을 쓰면 친구 요청을 보내고, 먼저 요청을 보낸 사람이면 수락하고,
  앱을 안 쓰면 초대 링크를 권한다. 앱을 쓰는지는 서버에 묻는다.
- 라운드 탭은 전반·후반·연장으로 나눠 라운드마다 진영, 끝난 방식, 내 킬과 퍼블·퍼데를 적는다. 못 뛴
  라운드도 승패는 남긴다. 에이스와 이긴 클러치("1대3 클러치")는 킬 앞에 `--t1` SemiBold로 적는다. 진 클러치는
  적지 않는다. 한 줄에 안 들어가면 오른쪽 꼬리를 설명 밑으로 내린다.
- 이코노미 탭은 풀바이·포스바이·이코마다 몇 라운드를 이겼는지, 그리고 라운드마다 우리 팀 평균 장비
  가치와 상대 평균을 적는다.

## 에셋

이미지는 Riot이 공개한 콘텐츠 카탈로그에서 가져온다. 직접 그린 아이콘은 네비게이션과 컨트롤에만 쓴다.

우리 서버가 카탈로그를 그대로 올려 두고 ID로 찾게 하는 것이 기본이고, 앱에 넣은 이미지는 서버에서
못 받았을 때 대신 쓴다. op.gg도 카탈로그 파일 이름 그대로 자기 CDN에 올려 쓴다. 앱에 넣는 이미지는
`tools/bundle_assets.py`가 골라 줄인다. 요원 얼굴, 역할 아이콘, 맵(목록용·배너), 무기, 티어 엠블럼이다. 파일 이름은 영문 이름(`agents/jett.png`)이고, UUID에서 파일로 가는
표는 스크립트가 `GameAssetIndex.kt`로 만든다. 그 파일은 손으로 고치지 않는다.

- 무기는 카탈로그 무기 폴더의 흰 선화가 아니라 기본 스킨 그림을 쓴다. 선화는 라이트 테마에서 안
  보인다. 기본 스킨이 없는 모드 전용 무기와 스킬 장비는 넣지 않는다. 화면에는 그 그림을 `--t2` 한 가지로 칠한
  실루엣으로 띄우고 면을 깔지 않는다(`WeaponImage`의 `tint`). 왼쪽 끝을 옆 글자와 맞춘다.
- 요원 얼굴은 op.gg처럼 경기 기록(경기 줄, S3 스코어보드, S7 요원)에만 쓴다. 그 판에 고른 요원이지 그
  사람을 나타내는 사진이 아니다.
- 아바타(홈 오른쪽 위, 프로필 머리, 친구 목록)는 플레이어 카드 자리다. 카드를 서버에서 받기 전까지 Riot ID
  첫 글자를 띄우고 기본 이미지는 두지 않는다.
- 플레이어 카드는 경기 응답의 `players[].playerCard`로 안다. 프로필 조회 API가 없어 가장 최근 경기의
  카드를 쓴다. 1,016장이라 앱에 넣지 않고 서버에서 받는다. 그때까지 프로필 배너는 면이다.
- 티어 엠블럼은 카탈로그에 없어 valorant-api.com에서 받았다.

카탈로그는 패치 뒤 사람 손으로 갱신되니 새 요원은 이름 첫 글자로 대체한다.
화면에 띄우는 이름을 ID나 숫자에 잇는 표는 앱 코드에 하드코딩하지 않는다.
